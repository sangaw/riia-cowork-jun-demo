app:
  name: "rita"
  version: "1.0.0"

server:
  host: "0.0.0.0"
  port: 8000

data:
  input_dir: "rita_input"
  output_dir: "rita_output"

model:
  path: "rita_output/models"

# Financial constants — config-driven, never hardcoded in application code
instruments:
  nifty:
    lot_size: 75        # changed from 50 in 2024
  banknifty:
    lot_size: 30
