To test on your Android device right now

Step 1 — Serve the folder (run this in your terminal):
cd "C:/Users/Sandeep/Documents/Work/code/riia-cowork-jun/rita-build-portfolio/android-mobile-app"
python -m http.server 8080

Step 2 — Find your computer's IP (run in a second terminal):
ipconfig
Look for IPv4 Address under your WiFi adapter — e.g. 192.168.1.42

Step 3 — Open in Chrome on Android

Make sure your phone is on the same WiFi, then go to:
http://192.168.1.42:8080

Step 4 — Install as app

Chrome will show a banner at the bottom: "Add RITA to Home screen" — tap it. It installs as a full-screen standalone app with no browser chrome.