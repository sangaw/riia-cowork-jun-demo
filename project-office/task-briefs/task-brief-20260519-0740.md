# Task Brief — 20260519-0740
**Run ID:** 20260519-0740
**Status:** in-progress

---

## Request
Relocate agent-ops data files (metrics.json, runs/*.json) from riia-ai-org/ into riia-jun-release/data/agent-ops/ and utility scripts to project-office/scripts/agent-ops/. Update hardcoded riia-ai-org paths in ops.py (two occurrences: lines 453 and 577). Drop riia-ai-org/ from git tracking by adding to .gitignore and running git rm --cached. Full requirements: project-office/features/May/12 Restructure ai org project/REQUIREMENTS.md

## App Target
ops

## Skill Selected
`project-office/skills/skill-add-ops-feature.md`

## Spec Reference
- `project-office/specs/Spec_RITA_App.md`
- `project-office/specs/Spec_JS_Code.md`

---

## [PM] Validation

**Sprint alignment:** In scope. All v1.0 sprints are complete and the project is in a post-release improvement phase. Feature 12 is a housekeeping / path-fix task directly supporting production deployability — it removes a hard dependency on the non-deployable `riia-ai-org/` POC folder. This work is consistent with the ongoing feature backlog being executed via `/enhance` and does not conflict with any open sprint task.

**Risk flags:**
- Path index arithmetic is brittle — `parents[N]` counts must be recalculated carefully for both `ops.py` and the relocated scripts; an off-by-one error will silently read from a wrong directory. REQUIREMENTS.md §6 provides the exact corrected indices, which must be followed precisely.
- `git rm --cached -r riia-ai-org/` must be the final step, executed only after data files are confirmed present in the new location; running it prematurely would remove the source files from the working tree on machines where `.gitignore` is not yet in effect.
- The four utility scripts (`aggregate_metrics.py`, `write_run_to_db.py`, `backfill_metrics.py`, `seed_agent_builds.py`) each carry their own path resolutions; all four must be updated, not just the one called out by name in the brief summary.

**Dependencies:** none — `ops.py` endpoints (`/api/experience/ops/agent-builds`, `/api/experience/ops/token-forecast`) are already live; no schema, migration, or DB changes are required.

**Cross-system scope check:**
- Does this feature affect the RITA dashboard (rita.html / dashboard/js/rita/)? No — this feature only changes backend path resolution in `ops.py` and relocates data files; no RITA JS or HTML is touched.
- Does this feature affect the FnO dashboard (fno.html / dashboard/js/fno/)? No — FnO has no dependency on `agent-ops` data paths or `ops.py`.
- Does this feature affect the DS dashboard (ds.html)? No — DS has no dependency on agent-ops data.
- Does this feature affect the Mobile PWA (rita-build-portfolio/android-mobile-app/index.html)? No — the mobile app does not call agent-ops endpoints.
- Does this feature affect the Ops dashboard JS (dashboard/js/ops/)? No — REQUIREMENTS.md §2 explicitly states the Ops JS does not need changes; `agent-builds.js` calls the same API endpoints, which resolve the new path transparently on the backend.

**Approved to proceed:** yes

> If no: state reason and stop. Do not spawn Architect agent.

---

## [Architect] Design

### 1. Feature Summary
This migration removes a hard dependency on the non-deployable `riia-ai-org/` POC folder by relocating its operational data files into `riia-jun-release/data/agent-ops/` (so they deploy with the app) and its utility scripts into `project-office/scripts/agent-ops/` (project tooling). Two hardcoded path expressions in `ops.py` are corrected so the two live agent-ops API endpoints resolve data from the new production location, and `riia-ai-org/` is dropped from git tracking entirely.

### 2. API Contract
No contract change — internal path fix only.

The two existing endpoints whose internal file-resolution logic is being corrected:
- GET /api/experience/ops/agent-builds
- GET /api/experience/ops/token-forecast

### 3. Frontend Target
No frontend changes — the Ops JS calls the same endpoints, which resolve the new path transparently on the backend. JS module that would reveal regression: `dashboard/js/ops/agent-builds.js` — no edits required.

### 4. Files to Touch

| File | Change |
|---|---|
| `riia-jun-release/src/rita/api/experience/ops.py` | Fix 2 hardcoded path expressions: (A) line 453 `parents[5] / "riia-ai-org" / "agent-ops"` → `parents[4] / "data" / "agent-ops"`; (B) line 576-577 eliminate `repo_root` var, replace `metrics_path` with `Path(__file__).parents[4] / "data" / "agent-ops" / "metrics.json"` |
| `riia-jun-release/data/agent-ops/metrics.json` | Create dir + copy from `riia-ai-org/agent-ops/metrics.json` |
| `riia-jun-release/data/agent-ops/runs/*.json` | Create dir with `.gitkeep`, copy all 40 run JSONs from `riia-ai-org/agent-ops/runs/` |
| `project-office/scripts/agent-ops/aggregate_metrics.py` | Copy from `riia-ai-org/agent-ops/`; fix `main()`: replace `script_dir / "runs"` + `script_dir / "metrics.json"` with `parents[3] / "riia-jun-release" / "data" / "agent-ops"` derivations |
| `project-office/scripts/agent-ops/write_run_to_db.py` | Copy from `riia-ai-org/agent-ops/`; fix line 10: `_repo_root = _script_dir.parents[1]` → `parents[2]` |
| `project-office/scripts/agent-ops/backfill_metrics.py` | Copy from `riia-ai-org/agent-ops/`; fix `main()`: replace `script_dir / "runs"` with `parents[3] / "riia-jun-release" / "data" / "agent-ops" / "runs"` |
| `project-office/scripts/agent-ops/seed_agent_builds.py` | Copy from `riia-ai-org/agent-ops/`; fix module-level: `parents[2]` → `parents[3]`; fix `RUNS_DIR` from `riia-ai-org/...` to `riia-jun-release/data/agent-ops/runs` |
| `.gitignore` | Append `riia-ai-org/` entry |
| git index | Run `git rm --cached -r riia-ai-org/` as final step (does not delete files from disk) |

### 5. Edge Cases to Handle
- Destination directory `riia-jun-release/data/agent-ops/` does not yet exist — create with `.gitkeep` in `runs/`
- metrics.json missing after copy would cause token-forecast to return 503 — verify file exists before testing
- `git rm --cached` must be last step, after all files confirmed at new location
- Each script has a different path-depth pattern — must be fixed individually (not a blanket "+1")
- `seed_agent_builds.py` has module-level `RUNS_DIR` (executes at import time) with a hardcoded `riia-ai-org` reference — both `parents[N]` and `RUNS_DIR` path must be fixed in the same module-level block
- After removing `repo_root` from `get_token_forecast`, confirm no other code in that function uses `repo_root` — an undefined-name ruff error would fail the ruff gate

### 6. Definition of Done Checklist
- [ ] GET /api/experience/ops/agent-builds returns 200 with non-empty metrics_extra (all 7 KPI sections populated)
- [ ] GET /api/experience/ops/token-forecast returns 200 (not 503) with complexity forecast payload
- [ ] ruff check riia-jun-release/src/rita/api/experience/ops.py passes with zero errors
- [ ] aggregate_metrics.py runs without error and writes to riia-jun-release/data/agent-ops/metrics.json
- [ ] write_run_to_db.py runs without import errors
- [ ] riia-jun-release/data/agent-ops/metrics.json exists and contains all 7 KPI sections
- [ ] riia-jun-release/data/agent-ops/runs/ contains all run JSON files (count matches source)
- [ ] riia-ai-org/ is listed in .gitignore
- [ ] git status shows no tracked files under riia-ai-org/
- [ ] No references to riia-ai-org remain in any file under riia-jun-release/
- [ ] Spec files not affected — no API contract or JS module changes (Spec_RITA_App.md and Spec_JS_Code.md require no updates)

---

## [Engineer] Implementation Log

**Branch:** `worktree-agent-abd81397609ffa6d0`

**Worktree path:** `C:/Users/Sandeep/Documents/Work/code/riia-cowork-jun/.claude/worktrees/agent-abd81397609ffa6d0`

**Commit hash:** `ad3bc09`

**Files changed:**
| File | Change made |
|---|---|
| `riia-jun-release/data/agent-ops/metrics.json` | Created — moved from riia-ai-org/agent-ops/ |
| `riia-jun-release/data/agent-ops/runs/*.json` | Created — 41 run JSONs moved from riia-ai-org/agent-ops/runs/ |
| `riia-jun-release/data/agent-ops/runs/.gitkeep` | Created — tracks empty runs/ dir in git |
| `riia-jun-release/src/rita/api/experience/ops.py` | Fixed 2 paths: get_agent_builds parents[5]/riia-ai-org → parents[4]/data; get_token_forecast eliminated repo_root var, metrics_path → parents[4]/data/agent-ops/metrics.json |
| `riia-jun-release/src/rita/api/experience/invest_game.py` | Fixed _AGENT_OPS_RUNS: 6×parent/riia-ai-org/agent-ops/runs → parents[4]/data/agent-ops/runs |
| `riia-jun-release/src/rita/main.py` | Fixed _agent_ops_dir static mount: riia-ai-org/agent-ops → data/agent-ops |
| `project-office/scripts/agent-ops/aggregate_metrics.py` | Moved from riia-ai-org; main() uses parents[3]/riia-jun-release/data/agent-ops for runs_dir + output_path |
| `project-office/scripts/agent-ops/write_run_to_db.py` | Moved from riia-ai-org; _repo_root parents[1] → parents[2] |
| `project-office/scripts/agent-ops/backfill_metrics.py` | Moved from riia-ai-org; main() uses parents[3]/riia-jun-release/data/agent-ops for runs_dir |
| `project-office/scripts/agent-ops/seed_agent_builds.py` | Moved from riia-ai-org; repo_root parents[2]→parents[3]; RUNS_DIR → riia-jun-release/data/agent-ops/runs |
| `.gitignore` | Appended riia-ai-org/ entry |
| git index | `git rm --cached -r riia-ai-org/` executed — all riia-ai-org tracked files removed |

**API contract verified:** n/a — no new contract; existing endpoints internal path fix only

**Spec updated:** n/a — no API contract or JS module changes per Architect design; spec files require no updates

**Ruff result:** passed — `ruff check src/rita/api/experience/ops.py src/rita/api/experience/invest_game.py src/rita/main.py` returned "All checks passed!"

**Definition of Done — Engineer check:**
- [x] API contract matches — n/a (no new contract; existing endpoints unchanged externally)
- [x] Experience tier read-only — ops.py endpoints already read-only; no db.commit() introduced
- [x] Section loader registered — n/a (no new JS module)
- [x] Window bindings set — n/a (no new JS module)
- [x] Error handled — n/a (no JS changes)
- [x] Spec updated — n/a (Architect confirmed no spec changes needed; zero spec rows in files-to-touch)
- [x] Ruff passes — all checks passed on ops.py, invest_game.py, main.py
- [x] No hardcoded values — all riia-ai-org references removed from riia-jun-release/src/ (grep returned empty)

> Engineer section complete. Branch: worktree-agent-abd81397609ffa6d0. Commit: ad3bc09. DoD: 8/8 passed.

---

## [QA] Test Results

**Tests written:** {n}

**Test file:** `tests/{unit|integration}/{name}_test.py`

**Tests passed:** {n} / {n}

**Coverage delta:** {+/- n%} (run: `pytest --cov=src/rita tests/`)

**Contract check:** {list each response field in schema vs handler return — match | mismatch}
| Schema field | Handler returns | Match? |
|---|---|---|
| {field} | {value/type} | {yes \| no} |

**Edge cases tested:**
- {list each edge case from Architect section — tested? yes/no}

**Definition of Done — QA check:**
- [ ] At least 1 unit test per new endpoint
- [ ] All tests pass
- [ ] API-frontend contract verified (no field name mismatches)
- [ ] Edge cases covered

---

## [TechWriter] Documentation

**Confluence page updated:** {URL | n/a — reason}

**Page section modified:** {section name on the Confluence page}

**Spec file confirmed current:** {yes | no — if no, update before closing}

**Task brief archived:** {yes — file renamed from TEMPLATE to task-brief-{timestamp}.md}

**Definition of Done — TechWriter check:**
- [ ] Confluence page reflects new feature
- [ ] Spec_RITA_App.md endpoint inventory is accurate
- [ ] Spec_JS_Code.md module structure table is accurate
- [ ] Task brief saved as completed artifact

---

*Brief generated by /enhance orchestrator. Each section written by the designated agent role.*
