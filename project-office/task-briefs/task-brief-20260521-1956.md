# Task Brief — 20260521-1956
**Run ID:** 20260521-1956
**Status:** in-progress

---

## Request
Feature 18 — User Traffic Dashboard: anonymised login event logging and KPI page showing total users, active users, and a 30-day activity breakdown

## App Target
ops (new standalone page + ops nav link)

## Skill Selected
`project-office/skills/skill-add-ops-feature.md`

## Spec Reference
- `project-office/specs/Spec_RITA_App.md`
- `project-office/specs/Spec_JS_Code.md`

---

## [PM] Validation

**Sprint alignment:** In scope. Post-release enhancement track.

**Risk flags:**
- MEDIUM — New ORM model (LoginEventModel) + schema migration required. Alembic migration must be applied before app starts.
- LOW — Standalone page (`users.html`) rather than an ops section. JWT guard handled in JS module.

**Dependencies:** Feature 17 (google auth) must be complete — `google_callback` route must exist in `auth.py`.

**Approved to proceed:** yes

---

## [Architect] Design

**Feature summary:** Log every Google OAuth login as an anonymised event (email hash = user_id FK). Expose an Experience tier endpoint returning KPI summary + 30-day daily breakdown. Serve a standalone `users.html` page with 5 KPI tiles, a bar chart, and a daily table. Add a sidebar nav link in ops.html.

**API contract:**
| Field | Value |
|---|---|
| Method | GET |
| Path | `/api/v1/experience/users/traffic` |
| Query params | none |
| Request body | none |
| Response shape | `UserTrafficResponse` |
| Auth required | JWT (Bearer) |

**Response schema:**
```
UserTrafficResponse {
  summary: UserTrafficSummary {
    total_users: int
    active_today: int
    active_this_week: int
    active_this_month: int
    total_logins_all_time: int
  }
  daily: list[DailyTrafficRow] {
    date: str
    unique_users: int
    total_logins: int
    new_registrations: int
  }
}
```

**Frontend target:**
| Item | Value |
|---|---|
| JS module | `dashboard/js/users/main.js` (new standalone module) |
| HTML page | `dashboard/users.html` (new standalone page) |
| DOM element IDs | ut-total-users, ut-active-today, ut-active-week, ut-active-month, ut-total-logins, ut-logins-chart, ut-daily-tbody |
| Window bindings | n/a — DOMContentLoaded only |

**Files to touch:**
| File | Change |
|---|---|
| `src/rita/models/login_event.py` | NEW — LoginEventModel ORM |
| `src/rita/models/__init__.py` | Add LoginEventModel import + __all__ |
| `src/rita/models/user.py` | Add first_login_date column |
| `alembic/versions/20260521_add_login_events.py` | NEW — migration: login_events table + first_login_date column |
| `src/rita/api/v1/auth.py` | Insert login event + first_login_date logic in google_callback |
| `src/rita/repositories/login_event.py` | NEW — LoginEventRepository |
| `src/rita/schemas/user_traffic.py` | NEW — UserTrafficSummary, DailyTrafficRow, UserTrafficResponse |
| `src/rita/api/experience/users.py` | NEW — GET /api/v1/experience/users/traffic |
| `src/rita/main.py` | Register users_traffic_router in Experience Layer block |
| `dashboard/js/users/main.js` | NEW — standalone JS module for users.html |
| `dashboard/users.html` | NEW — standalone KPI page |
| `dashboard/ops.html` | Add nav item for User Traffic in Access section |
| `project-office/specs/Spec_RITA_App.md` | Add endpoint row to Experience tier table |
| `project-office/specs/Spec_JS_Code.md` | Add module row to JS module table |

**Definition of Done checklist:**
- [ ] API contract matches — schema field names match JS reads
- [ ] Experience tier read-only — no db.commit() in users.py
- [ ] Migration applied — alembic upgrade head confirmed
- [ ] Login event written in google_callback
- [ ] Standalone users.html renders without JS errors
- [ ] Ops nav link added
- [ ] Spec updated
- [ ] Ruff passes

---

## [Engineer] Implementation Log

**Branch:** `worktree-agent-a0aa844b50ab94e25`

**Worktree path:** `C:/Users/Sandeep/Documents/Work/code/riia-cowork-jun/.claude/worktrees/agent-a0aa844b50ab94e25`

**Commit hash:** `8dd8796`

**Files changed:**
| File | Change made |
|---|---|
| `riia-jun-release/src/rita/models/login_event.py` | NEW — LoginEventModel ORM (login_events table) |
| `riia-jun-release/src/rita/models/__init__.py` | Added LoginEventModel import and __all__ entry |
| `riia-jun-release/src/rita/models/user.py` | Added first_login_date column (nullable DateTime) |
| `riia-jun-release/alembic/versions/20260521_add_login_events.py` | NEW — migration: creates login_events, adds first_login_date to users |
| `riia-jun-release/src/rita/api/v1/auth.py` | Added uuid import + LoginEventModel import; login event write + first_login_date set in google_callback |
| `riia-jun-release/src/rita/repositories/login_event.py` | NEW — LoginEventRepository with KPI queries and 30-day breakdown |
| `riia-jun-release/src/rita/schemas/user_traffic.py` | NEW — UserTrafficSummary, DailyTrafficRow, UserTrafficResponse Pydantic schemas |
| `riia-jun-release/src/rita/api/experience/users.py` | NEW — GET /api/v1/experience/users/traffic experience endpoint |
| `riia-jun-release/src/rita/main.py` | Imported and registered users_traffic_router in Experience Layer block |
| `riia-jun-release/dashboard/js/users/main.js` | NEW — standalone JS module (fetch, KPI DOM update, Chart.js bar, table render) |
| `riia-jun-release/dashboard/users.html` | NEW — standalone User Traffic KPI page with 5 tiles, chart, table |
| `riia-jun-release/dashboard/ops.html` | Added User Traffic `<a>` nav link in Access sidebar section |
| `project-office/specs/Spec_RITA_App.md` | Added users.py router row + Users Experience Endpoints table section |
| `project-office/specs/Spec_JS_Code.md` | Added Section 5 — dashboard/js/users/ module table |
| `project-office/task-briefs/task-brief-20260521-1956.md` | Created task brief |

**API contract verified:** yes — all JS `data.field` accesses (s.total_users, s.active_today, s.active_this_week, s.active_this_month, s.total_logins_all_time, r.date, r.unique_users, r.total_logins, r.new_registrations) match exact Pydantic field names

**Spec updated:** yes — Spec_RITA_App.md and Spec_JS_Code.md both updated

**Ruff result:** passed (1 unused import fixed before commit)

**Definition of Done — Engineer check:**
- [x] API contract matches — all JS field reads verified against Pydantic schema
- [x] Experience tier read-only — no db.commit() in users.py
- [x] Section loader registered — n/a (standalone page, not an ops section)
- [x] Window bindings set — n/a (standalone DOMContentLoaded module, no onclick handlers)
- [x] Error handled — try/catch in loadUserTraffic(); shows — on all KPI tiles on failure
- [x] Spec updated — endpoint + module rows added to both spec files
- [x] Ruff passes — all checks passed
- [x] No hardcoded values — uses window.RITA_API_BASE || "" throughout

---

## [QA] Test Results

**Tests written:** {n}

**Test file:** `tests/{unit|integration}/{name}_test.py`

**Tests passed:** {n} / {n}

**Coverage delta:** {+/- n%}

---

## [TechWriter] Documentation

**Confluence page updated:** {URL | n/a}

**Spec file confirmed current:** {yes | no}

**Task brief archived:** {yes}

---

*Brief generated by /enhance orchestrator.*
