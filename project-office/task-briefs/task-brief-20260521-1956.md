# Task Brief — 20260521-1956
**Run ID:** 20260521-1956
**Status:** in-progress

---

## Request
Feature 18 — User Traffic Dashboard: anonymised login event logging and KPI page showing total users, active users, and a 30-day activity breakdown

## App Target
ops (new standalone page + ops nav link)

## Skill Selected
`project-office/skills/skill-add-ops-feature.md`

## Spec Reference
- `project-office/specs/Spec_RITA_App.md`
- `project-office/specs/Spec_JS_Code.md`

---

## [PM] Validation

**Sprint alignment:** In scope. Post-release enhancement track.

**Risk flags:**
- MEDIUM — New ORM model (LoginEventModel) + schema migration required. Alembic migration must be applied before app starts.
- LOW — Standalone page (`users.html`) rather than an ops section. JWT guard handled in JS module.

**Dependencies:** Feature 17 (google auth) must be complete — `google_callback` route must exist in `auth.py`.

**Approved to proceed:** yes

---

## [Architect] Design

**Feature summary:** Log every Google OAuth login as an anonymised event (email hash = user_id FK). Expose an Experience tier endpoint returning KPI summary + 30-day daily breakdown. Serve a standalone `users.html` page with 5 KPI tiles, a bar chart, and a daily table. Add a sidebar nav link in ops.html.

**API contract:**
| Field | Value |
|---|---|
| Method | GET |
| Path | `/api/v1/experience/users/traffic` |
| Query params | none |
| Request body | none |
| Response shape | `UserTrafficResponse` |
| Auth required | JWT (Bearer) |

**Response schema:**
```
UserTrafficResponse {
  summary: UserTrafficSummary {
    total_users: int
    active_today: int
    active_this_week: int
    active_this_month: int
    total_logins_all_time: int
  }
  daily: list[DailyTrafficRow] {
    date: str
    unique_users: int
    total_logins: int
    new_registrations: int
  }
}
```

**Frontend target:**
| Item | Value |
|---|---|
| JS module | `dashboard/js/users/main.js` (new standalone module) |
| HTML page | `dashboard/users.html` (new standalone page) |
| DOM element IDs | ut-total-users, ut-active-today, ut-active-week, ut-active-month, ut-total-logins, ut-logins-chart, ut-daily-tbody |
| Window bindings | n/a — DOMContentLoaded only |

**Files to touch:**
| File | Change |
|---|---|
| `src/rita/models/login_event.py` | NEW — LoginEventModel ORM |
| `src/rita/models/__init__.py` | Add LoginEventModel import + __all__ |
| `src/rita/models/user.py` | Add first_login_date column |
| `alembic/versions/20260521_add_login_events.py` | NEW — migration: login_events table + first_login_date column |
| `src/rita/api/v1/auth.py` | Insert login event + first_login_date logic in google_callback |
| `src/rita/repositories/login_event.py` | NEW — LoginEventRepository |
| `src/rita/schemas/user_traffic.py` | NEW — UserTrafficSummary, DailyTrafficRow, UserTrafficResponse |
| `src/rita/api/experience/users.py` | NEW — GET /api/v1/experience/users/traffic |
| `src/rita/main.py` | Register users_traffic_router in Experience Layer block |
| `dashboard/js/users/main.js` | NEW — standalone JS module for users.html |
| `dashboard/users.html` | NEW — standalone KPI page |
| `dashboard/ops.html` | Add nav item for User Traffic in Access section |
| `project-office/specs/Spec_RITA_App.md` | Add endpoint row to Experience tier table |
| `project-office/specs/Spec_JS_Code.md` | Add module row to JS module table |

**Definition of Done checklist:**
- [ ] API contract matches — schema field names match JS reads
- [ ] Experience tier read-only — no db.commit() in users.py
- [ ] Migration applied — alembic upgrade head confirmed
- [ ] Login event written in google_callback
- [ ] Standalone users.html renders without JS errors
- [ ] Ops nav link added
- [ ] Spec updated
- [ ] Ruff passes

---

## [Engineer] Implementation Log

**Branch:** {to be filled}

**Worktree path:** {to be filled}

**Commit hash:** {to be filled}

**Files changed:**
| File | Change made |
|---|---|
| {file} | {what was changed} |

**API contract verified:** {yes | no}

**Spec updated:** {yes | no}

**Ruff result:** {passed | failed}

**Definition of Done — Engineer check:**
- [ ] API contract matches
- [ ] Experience tier read-only
- [ ] Section loader registered
- [ ] Window bindings set
- [ ] Error handled
- [ ] Spec updated
- [ ] Ruff passes
- [ ] No hardcoded values

---

## [QA] Test Results

**Tests written:** {n}

**Test file:** `tests/{unit|integration}/{name}_test.py`

**Tests passed:** {n} / {n}

**Coverage delta:** {+/- n%}

---

## [TechWriter] Documentation

**Confluence page updated:** {URL | n/a}

**Spec file confirmed current:** {yes | no}

**Task brief archived:** {yes}

---

*Brief generated by /enhance orchestrator.*
