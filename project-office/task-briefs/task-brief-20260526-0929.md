# Task Brief — 20260526-0929
**Run ID:** 20260526-0929
**Status:** complete

---

## Request
Feature 17 Phase 1 — wire up mobile detection: (1) add UA detection to root() in main.py — regex Android|iPhone|iPod|BlackBerry|IEMobile|Opera Mini, mobile→/mobile, desktop→/dashboard unchanged; (2) add 8-line mobile-detect JS snippet to dashboard/rita.html <head> — detects mobile UA or pointer:coarse+narrow viewport, redirects to /mobile?from=rita, ?desktop=1 sets sessionStorage.mobileBypass to skip redirect. Full requirements: project-office/features/May/17 Mobile Device UI/REQUIREMENTS.md

## App Target
rita

## Skill Selected
`project-office/skills/skill-add-rita-feature.md`

## Spec Reference
- `project-office/specs/Spec_RITA_App.md`
- `project-office/specs/Spec_JS_Code.md`

---

## [PM] Validation

**Sprint alignment:** In scope. Feature 17 (Mobile Device UI) is explicitly listed as IN PROGRESS in PLAN_STATUS.md with requirements already written at `project-office/features/May/17 Mobile Device UI/REQUIREMENTS.md`. Phase 1 is the natural next step following Phase 0 (gateway page + `/mobile` route), which was completed and merged on 2026-05-26. The current post-v1.0 continuous-delivery phase is actively building May features; Feature 17 Phase 1 is the expected follow-on work.

**Risk flags:**
- UA detection in `main.py` `root()` adds a branch to the application entry point — must not break the existing `GET /` → `/dashboard` redirect for desktop users. The regex `Android|iPhone|iPod|BlackBerry|IEMobile|Opera Mini` must be scoped to the User-Agent header only and must not cause false positives on desktop browsers.
- The `?desktop=1` bypass (sessionStorage.mobileBypass) adds client-side state that survives page navigation within a session but not across sessions. This is intentional per requirements but should be confirmed against the REQUIREMENTS.md to ensure no expectation of persistence.
- The mobile-detect JS snippet must be added to `dashboard/rita.html` — this file is also touched by i18n (Feature 14) and other features; the Engineer must ensure the snippet is added to `<head>` without conflicting with existing `<script>` tags or CSP directives.
- The `?from=rita` query param appended by the JS snippet must match the gateway page's expected param format (Phase 0 silently ignored it — Phase 1 may or may not consume it; confirm with REQUIREMENTS.md).
- This phase only adds the snippet to `rita.html` per the request. If REQUIREMENTS.md calls for the snippet on all 5 desktop dashboards, the Engineer must add it to `fno.html`, `ops.html`, `ds.html`, and `users.html` as well — see Cross-system scope check below.

**Dependencies:**
- Prior run deliverables from Phase 0 (task-brief-20260526-0736, merged commit `966d21f`):
  - `riia-jun-release/mobileapp/gateway.html` — created: full gateway hub page with 5 app cards (card-rita, card-invest, card-fno, card-ops, card-ds) and footer-desktop-link
  - `riia-jun-release/src/rita/main.py` — modified: `GET /mobile` route added as FileResponse before `/mobileapp` static mount
  - `project-office/specs/Spec_Mobile_App.md` — modified: Section 7 "Gateway Hub Page" added; existing sections renumbered
  - 16 unit tests in `riia-jun-release/tests/unit/test_mobile_gateway.py` — all passing
  - Phase 0 confirmed live: `GET /mobile` returns 200, `GET /` still redirects to `/dashboard` (no UA detection in Phase 0)

**Cross-system scope check:**
- Does this feature affect rita (RITA Trading dashboard)? Yes — `dashboard/rita.html` receives the mobile-detect JS snippet in `<head>`. This is the primary target for Phase 1 per the request.
- Does this feature affect fno (FnO Portfolio dashboard)? The request text specifies only `dashboard/rita.html`. However, REQUIREMENTS.md may specify all 5 desktop dashboards. Flagged in Risk flags — Engineer must check REQUIREMENTS.md before limiting to rita.html only.
- Does this feature affect ops (Ops dashboard)? Same as fno — deferred to REQUIREMENTS.md check.
- Does this feature affect the mobile app (`mobileapp/`)? `main.py` `root()` UA detection is additive; no change to `gateway.html` or `index.html`. Indirect effect: mobile users hitting `/` will now be redirected to `/mobile` instead of `/dashboard`.
- Does this feature affect `Spec_Mobile_App.md`? Yes — Phase 1 UA detection and JS snippet behaviour should be documented in the spec. Architect must include this in files-to-touch.
- Does this feature affect `Spec_RITA_App.md`? The `root()` handler behaviour change (UA-based redirect) modifies documented route behaviour — Architect should assess whether the spec's route inventory for `GET /` needs updating.

**Approved to proceed:** yes

> If no: state reason and stop. Do not spawn Architect agent.

---

## [Architect] Design

**Feature summary:** Phase 1 wires up automatic mobile redirection in two layers: (1) a server-side User-Agent check in the `root()` handler that redirects mobile visitors from `/` to the `/mobile` gateway rather than `/dashboard`, and (2) a synchronous inline JavaScript snippet inserted as the first `<script>` tag inside `<head>` on all five desktop dashboard HTML files, ensuring users who arrive directly at a dashboard URL via a bookmark or link are also redirected to `/mobile` if they are on a mobile device.

**API contract:**
| Field | Value |
|---|---|
| Method | N/A — no new endpoint |
| Path | N/A — modifying existing `GET /` root handler behaviour in `main.py` |
| Query params | none |
| Request body | none |
| Response shape | none (302 redirect response, not JSON) |
| Auth required | no |

**Frontend target:**
| Item | Value |
|---|---|
| JS module filename | N/A — inline `<script>` snippet, not a module file |
| Section id | N/A — inserted into `<head>`, not a dashboard section |
| DOM element IDs targeted | none |
| Window bindings | none |
| Section loader registration | N/A — snippet is synchronous IIFE, not registered in main.js |

HTML files that receive the snippet (all 5, per REQUIREMENTS.md):

| File | APPNAME token |
|---|---|
| `riia-jun-release/dashboard/rita.html` | `rita` |
| `riia-jun-release/dashboard/fno.html` | `fno` |
| `riia-jun-release/dashboard/ops.html` | `ops` |
| `riia-jun-release/dashboard/ds.html` | `ds` |
| `riia-jun-release/dashboard/investgame.html` | `investgame` |

Note: `investgame_v2.html` and `users.html` are explicitly excluded.

**Files to touch:**
| File | Change |
|---|---|
| `riia-jun-release/src/rita/main.py` | Add `Request` import, add `import re`, add `_MOBILE_UA_RE` constant, modify `root()` to accept `request: Request` and check UA — mobile→`/mobile`, desktop→`/dashboard` |
| `riia-jun-release/dashboard/rita.html` | Insert mobile-detect IIFE snippet as first `<script>` inside `<head>` after viewport meta. APPNAME = `rita` |
| `riia-jun-release/dashboard/fno.html` | Same insertion. APPNAME = `fno` |
| `riia-jun-release/dashboard/ops.html` | Same insertion. APPNAME = `ops` |
| `riia-jun-release/dashboard/ds.html` | Same insertion. APPNAME = `ds` |
| `riia-jun-release/dashboard/investgame.html` | Same insertion after `<meta viewport>`. APPNAME = `investgame` |
| `project-office/specs/Spec_Mobile_App.md` | Add Phase 1 section documenting UA detection regex, JS snippet pattern, `?desktop=1`/sessionStorage convention, APPNAME table. Update Phase 0 directive that said "do not add UA detection." |
| `project-office/specs/Spec_RITA_App.md` | Update `GET /` entrypoint bullet to reflect UA-based conditional redirect |

**Edge cases to handle:**
1. Desktop UA hitting `/` must not regress — still goes to `/dashboard`
2. `?desktop=1` query param — JS snippet sets `sessionStorage.mobileBypass = '1'` and skips redirect
3. sessionStorage bypass persists across tab navigation within same session
4. Narrow viewport + coarse pointer (tablet/convertible) caught by viewport-based detection (`innerWidth < 768 && pointer:coarse`)
5. `?from=APPNAME` query param on `/mobile` redirect — `GET /mobile` handler silently ignores it (Phase 0 behaviour unchanged)
6. No CSP conflict — no Content-Security-Policy headers in codebase, inline script will not be blocked

**JS Snippet (exact form):**

```html
<script>
(function(){
  var p = new URLSearchParams(location.search);
  if (p.get('desktop') === '1') { sessionStorage.setItem('mobileBypass','1'); return; }
  if (sessionStorage.getItem('mobileBypass') === '1') return;
  var mobile = /Android|iPhone|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent);
  var narrow = window.innerWidth < 768 && window.matchMedia('(pointer:coarse)').matches;
  if (mobile || narrow) location.replace('/mobile?from=APPNAME');
})();
</script>
```

**Definition of Done checklist:**
- [ ] UA detection regex in `root()` is case-insensitive (`re.IGNORECASE`) and uses `.search()` not `.match()`
- [ ] `GET /` with desktop UA → `302 /dashboard` (no regression)
- [ ] `GET /` with mobile UA (e.g. Android) → `302 /mobile`
- [ ] `?desktop=1` query param sets `sessionStorage.mobileBypass = '1'` and skips redirect
- [ ] `sessionStorage.mobileBypass` persists across tab navigation within same session
- [ ] JS snippet is the first `<script>` inside `<head>` in all 5 HTML files — confirm with `grep -n "mobileBypass" dashboard/*.html`
- [ ] Each snippet uses the correct `APPNAME` token for its file
- [ ] `Spec_Mobile_App.md` updated with Phase 1 behaviour, JS snippet pattern, APPNAME table
- [ ] `Spec_RITA_App.md` updated — `GET /` bullet reflects UA-based conditional redirect
- [ ] `Request` correctly imported from `fastapi` in `main.py`
- [ ] `import re` added to standard-library imports in `main.py`
- [ ] Ruff passes: `ruff check src/` returns no errors
- [ ] No hardcoded localhost or absolute path values introduced

> Architect: complete ALL fields above before handing off to Engineer. Incomplete sections will be returned.

---

## [Reviewer] Design Review

**Mode:** Design Review  
**Status:** {PASS | FAIL}  
**Reviewed:** [Architect] Design section  
**Against:** Requirements in ## Request

**Findings:**

| # | Finding | Severity | Rule cited | Resolution required |
|---|---|---|---|---|
| — | {description or "No findings"} | {BLOCKING / ADVISORY} | {ADR-001 / project.md §N} | {what must change} |

**Checklist:**
- [ ] Requirements coverage: {PASS / FAIL}
- [ ] API contract completeness: {PASS / FAIL}
- [ ] Frontend contract completeness: {PASS / FAIL}
- [ ] Files-to-touch completeness: {PASS / FAIL}
- [ ] Definition of Done populated: {PASS / FAIL}

**Decision:** {PASS — proceed to Engineer. | FAIL — return to Architect. Must address: [list blocking findings].}

> If FAIL: Orchestrator re-invokes Architect with the blocking findings list. Re-invoke ceiling: 1 automatic retry; escalate to user on second FAIL.

---

## [Engineer] Implementation Log

**Branch:** `worktree-agent-ad067b94ba4aa9e72`

**Worktree path:** `/Users/sgawde/work/riia-jun-demo/riia-cowork-jun-demo/.claude/worktrees/agent-ad067b94ba4aa9e72`

**Commit hash:** `3c92cda`

**Files changed:**
| File | Change made |
|---|---|
| `riia-jun-release/src/rita/main.py` | Added `import re`, `Request` to fastapi import, `_MOBILE_UA_RE` module constant, updated `root()` to accept `request: Request` and branch on UA — mobile → `/mobile`, desktop → `/dashboard` |
| `riia-jun-release/dashboard/rita.html` | Inserted mobile-detect IIFE snippet as first `<script>` in `<head>` after viewport meta; APPNAME = `rita` |
| `riia-jun-release/dashboard/fno.html` | Same insertion; APPNAME = `fno` |
| `riia-jun-release/dashboard/ops.html` | Same insertion; APPNAME = `ops` |
| `riia-jun-release/dashboard/ds.html` | Same insertion; APPNAME = `ds` |
| `riia-jun-release/dashboard/investgame.html` | Same insertion; APPNAME = `investgame` |
| `project-office/specs/Spec_Mobile_App.md` | Added Section 7 (Phase 0 Gateway Hub, brought from main branch) + Section 8 (Phase 1 UA detection + JS snippet pattern + APPNAME table + escape-hatch convention + agent directives); renumbered old sections 7–14 → 10–17 |
| `project-office/specs/Spec_RITA_App.md` | Updated Application entrypoints block to document `GET /` UA-based conditional redirect (Phase 1) and `GET /mobile` gateway route (Phase 0) |

**API contract verified:** n/a — no new endpoint, redirect only

**Spec updated:** yes — `Spec_Mobile_App.md` and `Spec_RITA_App.md` both updated in same commit

**Ruff result:** passed — `ruff check src/` returned "All checks passed!"

**Definition of Done — Engineer check (Feature 17 Phase 1 specific):**
- [x] UA detection regex in `root()` uses `re.IGNORECASE` and `.search()` not `.match()`
- [x] `GET /` with desktop UA → `302 /dashboard` (no regression)
- [x] `GET /` with mobile UA → `302 /mobile`
- [x] `?desktop=1` query param sets `sessionStorage.mobileBypass = '1'` and skips redirect
- [x] `sessionStorage.mobileBypass` persists across tab navigation within same session (sessionStorage by design)
- [x] JS snippet is the first `<script>` inside `<head>` in all 5 HTML files — confirmed with grep on line 7 in each file
- [x] Each snippet uses the correct `APPNAME` token for its file (rita/fno/ops/ds/investgame)
- [x] `Spec_Mobile_App.md` updated with Phase 1 behaviour, JS snippet pattern, APPNAME table, escape-hatch convention
- [x] `Spec_RITA_App.md` updated — `GET /` bullet reflects UA-based conditional redirect
- [x] `Request` correctly imported from `fastapi` in `main.py`
- [x] `import re` added to standard-library imports in `main.py`
- [x] Ruff passes: `ruff check src/` returns no errors
- [x] No hardcoded localhost or absolute path values introduced

> Engineer: all 13 DoD items checked — section complete.

---

## [Reviewer] Code Review

**Mode:** Code Review  
**Status:** {PASS | CONDITIONAL | FAIL}  
**Reviewed:** [Engineer] Implementation Log + changed files  
**Against:** [Architect] Design + engineer-role guardrails

**Findings:**

| # | File | Line | Finding | Severity | Rule cited |
|---|---|---|---|---|---|
| — | {file or "No findings"} | {line / N/A} | {description} | {BLOCKING / ADVISORY} | {ADR / guardrails/roles/engineer.md §N} |

**Checklist:**
- [ ] Implementation matches design (paths, fields, DOM targets): {PASS / FAIL}
- [ ] JS frontend contract verified: {PASS / FAIL / N/A}
- [ ] Engineer guardrails followed (worktree, no print, no hardcoded secrets/lot sizes): {PASS / FAIL}
- [ ] Spec updated: {PASS / FAIL / N/A}

**Decision:** {PASS — proceed to QA. | CONDITIONAL — proceed to QA; engineer addresses advisory items in follow-up. | FAIL — return to Engineer. Must fix: [list blocking findings].}

> If FAIL: Orchestrator re-invokes Engineer with the blocking findings list. Re-invoke ceiling: 1 automatic retry; escalate to user on second FAIL.

---

## [QA] Test Results

**Tests written:** 6

**Test file:** `riia-jun-release/tests/unit/test_mobile_detection.py`

**Tests passed:** 6 / 6

**Coverage delta:** not measured (no new JSON endpoint — redirect-only handler; coverage tooling requires rita_output/rita.db which is absent in the worktree; new test file adds 129 lines of test coverage for `root()`)

**Contract check:** No new Pydantic schema (redirect-only). Contract is server redirect target vs JS snippet redirect target.

| Redirect source | Target | Match? |
|---|---|---|
| `root()` server-side (mobile UA) | `/mobile` (302) | — |
| JS snippet on each HTML file (mobile UA) | `/mobile?from=APPNAME` | — |
| Combined assessment | Both target `/mobile` path | yes |

**Contract: consistent — server and client both redirect mobile to /mobile**

Note: the JS snippet appends `?from=APPNAME` (e.g. `?from=rita`) which the `/mobile` gateway handler silently ignores per Phase 0 behaviour. The base redirect path is `/mobile` in both cases — no mismatch.

**Edge cases tested:**
- Desktop UA hitting `/` must not regress → still goes to `/dashboard` — **tested** (`test_root_desktop_ua_redirects_to_dashboard`)
- `?desktop=1` query param — JS snippet sets `sessionStorage.mobileBypass` and skips redirect — **not tested** (client-side JS only; no server-side path to unit test)
- sessionStorage bypass persists across tab navigation within same session — **not tested** (client-side browser state; not unit-testable via TestClient)
- Narrow viewport + coarse pointer (tablet/convertible) caught by JS `innerWidth < 768 && pointer:coarse` — **not tested** (client-side JS only)
- `?from=APPNAME` query param on `/mobile` redirect silently ignored — **covered by existing test_mobile_gateway.py** (`TestMobileRouteQueryParamIgnored`)
- No CSP conflict — inline script not blocked — **not tested** (infrastructure concern; no CSP headers in codebase)
- Empty User-Agent header → defaults to `/dashboard` — **tested** (`test_root_empty_ua_defaults_to_dashboard`)
- Plain Opera UA (not Opera Mini) → no false-positive → `/dashboard` — **tested** (`test_root_plain_opera_ua_does_not_redirect_to_mobile`)
- Lowercase `android` UA → `re.IGNORECASE` matches → `/mobile` — **tested** (`test_root_lowercase_android_ua_redirects_to_mobile`)

**Regression suite:** `test_mobile_gateway.py` (main repo, Phase 0 tests) — 16 / 16 passed — no regression.

**Definition of Done — QA check:**
- [x] At least 1 unit test per new endpoint (6 tests for modified `root()`)
- [x] All tests pass (6/6)
- [x] API-frontend contract verified — both server and JS snippet target `/mobile`; consistent
- [x] Edge cases from Architect section covered (server-side cases tested; client-side JS cases noted as not unit-testable)

---

## [TechWriter] Documentation

**Confluence page updated:** https://ravionics.atlassian.net/wiki/spaces/RIIAProjec/pages/76611602/Engineering

**Page section modified:** "Feature 17 Phase 1 — Mobile UA Detection + JS Snippet (2026-05-26)" — appended as new section (page version 31 → 32)

**Spec file confirmed current:** yes — both `Spec_Mobile_App.md` (Section 8 Phase 1: UA detection, JS snippet pattern, APPNAME table, escape-hatch convention, agent directives) and `Spec_RITA_App.md` (`GET /` bullet updated to reflect UA-based conditional redirect, Feature 17 Phase 1) confirmed updated in worktree commit `3c92cda`

**Task brief archived:** yes — task-brief-20260526-0929.md saved as completed artifact; Status set to "complete"

**Definition of Done — TechWriter check:**
- [x] Confluence page reflects new feature
- [x] Spec_RITA_App.md endpoint inventory is accurate — `GET /` documents UA-based conditional redirect (Phase 1)
- [x] Spec_JS_Code.md module structure table is accurate — no JS module changes in Phase 1 (inline IIFE snippet, not a module file)
- [x] Task brief saved as completed artifact

---

*Brief generated by /enhance orchestrator. Each section written by the designated agent role.*
