# Task Brief — 20260604-1403
**Run ID:** 20260604-1403
**Status:** in-progress

---

## Request
improve the overview page. The instrument shown should show similar to what we have in RITA overview page. By default, ASML can be selected and then corresponding data can be loaded. Then we can show positions grid. Please remove paper toggle as this will be set on dashboard page and not here. We do not require equity hedge positions grid again as we have active positions. we can remove closed positions. Portfolio widget can be merged with widgets shown at the top. region allocation, strategy and total allocation is not needed as it is repeating the information. As part of last run for the enhance, I was expecting page updates for Risk, Scenarios, Hedge Radar and Manoeuvre. But those pages do not see any improvements

## App Target
fno

## Skill Selected
`project-office/skills/skill-add-fno-feature.md`

## Spec Reference
- `project-office/specs/Spec_RITA_App.md`
- `project-office/specs/Spec_JS_Code.md`

---

## [PM] Validation

**Sprint alignment:** In scope. PLAN_STATUS.md identifies F30 as the active feature with Phases 3–5 (Positions tab re-skin, Manoeuvre tab re-wire, Stress/Scenarios updates) marked READY after Phase 2. This request addresses two parallel concerns that both fall within F30's stated scope: (1) improving the Overview page instrument display and cleaning up redundant widgets, and (2) completing the Risk, Scenarios, Hedge Radar, and Manoeuvre page improvements that were expected from the F30 Phase 2 run but did not materialise. Both are FnO dashboard improvements using the portfolio-analytics endpoint already delivered in Phases 1–2. No new sprint milestone is required.

**Risk flags:**
- Phase 2 (task-brief-20260604-1126, commit 3c19040) wired the portfolio-analytics endpoint to state and `initApp()` but the Engineer Log confirms only state.js, app-init.js, main.js, and fno.html were modified — no section-specific renderers (risk.js, scenarios.js, payoff.js, manoeuvre.js, hedge-radar.js) were updated to consume the new state fields. This is the root cause of the continuation issue and must be addressed in this run.
- The Overview page currently uses `my-portfolio.js` (F29 Phase 3 redesign, commit 64099e3). Instrument selection with ASML-as-default requires either a new section loader or a significant refactor of the existing component. The Architect must confirm whether to extend `my-portfolio.js` or introduce a new `fno-overview.js` module to avoid regressions to the F29 Phase 3 work.
- Removing the paper toggle from the Overview page must not disable it globally — F30 Phase 2 added the Real/Mock toggle to the sidebar (`fno.html`); the Overview-level paper toggle is a separate DOM element. The Architect must identify the exact element to remove without disrupting the sidebar toggle.
- Merging the Portfolio widget into the top KPI strip requires care: the current `my-portfolio.js` already renders a 5-card KPI strip and a holdings table. Merging additional widgets risks DOM ID collisions and layout regressions in the existing Overview section.

**Dependencies:**
- Prior run deliverables:
  - F30 Phase 1 — portfolio-analytics backend endpoint: merged at commit `a3ba34a` (task-brief-20260604-1048). Endpoint `GET /api/v1/experience/fno/portfolio-analytics?mode=real|mock` is available.
  - F30 Phase 2 — state + initApp refactor, Real/Mock toggle: merged at commit `7bfb90c` (task-brief-20260604-1126). `state.portfolioMeta`, `state.analyticsMode`, `initApp(mode)` single-fetch, and `window.toggleAnalyticsMode` are live. All 13 response fields are mapped to state but are NOT yet consumed by section renderers.
- F29 Phase 3 — FnO Overview redesign (commit 64099e3): `my-portfolio.js` and the Overview HTML block are the current baseline for the Overview page being modified in this run.
- F29 Phase 4 (spec updates): still PENDING; Spec_JS_Code.md and Spec_RITA_App.md may lag for F29-touched areas. Engineer must read source files directly.

**Cross-system scope check:**
- Does this feature affect the RITA main dashboard? No — all changes are confined to `dashboard/js/fno/` and `fno.html`. RITA dashboard (`rita.html`, `dashboard/js/rita/`) is not touched.
- Does this feature affect the Ops dashboard? No — Ops dashboard files (`ops.html`, `dashboard/js/ops/`) are not relevant to FnO overview or analytics rendering.
- Does this feature affect the Mobile PWA? No — `riia-jun-release/mobileapp/` is a separate PWA with its own FnO-equivalent views. No mobile files are in scope for this run.

**Approved to proceed:** yes

> If no: state reason and stop. Do not spawn Architect agent.

---

## [Architect] Design

**Feature summary:** F30 Phase 3 completes the FnO dashboard by (A) improving the Overview page with an ASML-default instrument selector card, a live positions grid replacing the removed paper toggle and closed-positions panel, and collapsing the existing five-KPI portfolio strip into the top-of-page overview strip; and (B) wiring the Risk, Scenarios, Hedge Radar, and Manoeuvre pages to actually consume the `state.portfolioMeta`, `state.scenarioLevels`, `state.payoffData`, and `state.hedgeQuality` fields that were mapped in Phase 2 but never rendered, fixing the data-shape mismatches that prevent those pages from displaying live data.

**API contract:**

No new endpoint — reusing `GET /api/v1/experience/fno/portfolio-analytics?mode=real|mock` from F30 Phase 1.

| Field | Value |
|---|---|
| Method | GET |
| Path | `/api/v1/experience/fno/portfolio-analytics` |
| Query params | `mode`: "real" \| "mock" |
| Request body | none |
| Auth required | yes — JWT |

**State fields consumed (from existing endpoint response):**
| State field | API response key | Shape | Pages that consume it |
|---|---|---|---|
| state.portfolioMeta | portfolio_meta | {name, total_value_eur, updated_at} | Overview (my-portfolio.js) |
| state.positions | positions | list[PositionItemSchema] — fields: und, full, exp, type, side, qty, allocation_pct, position_eur, avg, ltp, chg, pnl, currency, ann_vol_pct, region | Overview positions grid, Manoeuvre |
| state.marketData | market | dict[str, MarketEntrySchema] — fields: close, open, high, low, prevClose, chgFromOpen, chgFromPrev, date, shares, turnover, currency | Overview instrument selector |
| state.scenarioLevels | scenario_levels | dict[str, ScenarioLevelSchema] — {INSTRUMENT: {target, sl}} | Scenarios (rr.js adapter), Manoeuvre |
| state.payoffData | payoff | {portfolio: {labels, data}, hedged: {labels, data}} | Risk (payoff.js adapter) |
| state.stressData | stress | list[{label, move_pct, portfolio_pnl_eur, hedged_pnl_eur}] | Risk (stress.js — new render path) |
| state.hedgeQuality | hedge_quality | {positions: list[{instrument, hqs, hqs_tier, hedged, strategy, coverage_pct, note}]} | Hedge Radar |
| state.netGreeks | net_greeks | {delta, theta, vega} | Risk (greeks.js) |
| state.greeksData | greeks | list[GreekItemSchema] — fields: und, exp, hedge_type, delta, gamma, theta, vega, allocation_pct, ann_vol_pct | Risk (greeks.js) |

**Frontend target:**

**Module A — my-portfolio.js (EXTEND)**
| Item | Value |
|---|---|
| JS module | `dashboard/js/fno/my-portfolio.js` |
| Section id | `page-overview` |
| DOM elements to ADD | fno-overview-inst-selector, fno-overview-positions-grid, fno-overview-kpi-value, fno-overview-kpi-pnl, fno-overview-kpi-delta, fno-overview-kpi-positions |
| DOM elements to REMOVE | paper-mode-chk label block (fno.html lines ~525-529) |
| Window bindings | `window.fnoSelectInstrument` (new) |
| New functions | `renderOverviewFromState()`, `fnoSelectInstrument()` |

**Module B — rr.js (UPDATE)**
| Item | Value |
|---|---|
| JS module | `dashboard/js/fno/rr.js` |
| Section id | `page-scenarios` |
| DOM IDs | nifty-scen-section, banknifty-scen-section, nifty-rr-kpis, nifty-rr-views, banknifty-rr-kpis, banknifty-rr-views, rr-history, scen-summary-card |
| Change | normalization done in app-init.js (_normScenarioLevels) |

**Module C — payoff.js (UPDATE)**
| Item | Value |
|---|---|
| JS module | `dashboard/js/fno/payoff.js` |
| Section id | `page-risk` |
| DOM IDs | payoff-chart, payoff-chart-bnkn, payoff-nifty-wrap, payoff-bnkn-wrap |
| Change | detect portfolio-shape vs NIFTY/BANKNIFTY legacy shape |

**Module D — stress.js (UPDATE)**
| Item | Value |
|---|---|
| JS module | `dashboard/js/fno/stress.js` |
| Section id | `page-risk` |
| DOM IDs | stress-row, stress-events-row (new) |
| Change | add `renderAnalyticsStress()` for historical events from state.stressData |

**Module E — hedge.js (UPDATE)**
| Item | Value |
|---|---|
| JS module | `dashboard/js/fno/hedge.js` |
| Section id | `page-hedge` |
| DOM IDs | hqs-kpis, hqs-alert-banner, hqs-tier-card, hqs-tbody, hqs-footer, hqs-portfolio-tbody (new) |
| Change | add `renderPortfolioHedgeRadar()` for instrument-level state.hedgeQuality.positions |

**Module F — manoeuvre.js (UPDATE)**
| Item | Value |
|---|---|
| JS module | `dashboard/js/fno/manoeuvre.js` |
| Section id | `page-manoeuvre` |
| DOM IDs | man-month-tiles, man-tab-strip, man-group-panel, man-pool-container |
| Change | fix p.instrument → p.full ?? p.instrument ?? p.und at line 140 |

**Module G — app-init.js (UPDATE)**
| Item | Value |
|---|---|
| JS module | `dashboard/js/fno/app-init.js` |
| Change | add `_normScenarioLevels()` helper; call after state.scenarioLevels assignment; replace `renderHedgeRadar()` with `renderPortfolioHedgeRadar()` in `_renderAll()`; add `renderOverviewFromState()` to `_renderAll()` |

**Module H — greeks.js (UPDATE)**
| Item | Value |
|---|---|
| JS module | `dashboard/js/fno/greeks.js` |
| Section id | `page-risk` |
| Change | fix `renderGreeksTable()` to use g.und + g.hedge_type instead of g.full, and g.ann_vol_pct instead of g.iv |

**Files to touch:**
| File | Change |
|---|---|
| riia-jun-release/dashboard/js/fno/my-portfolio.js | Add `renderOverviewFromState()`, `fnoSelectInstrument()`, `_selectedInstrument='ASML'` variable; export `renderOverviewFromState` |
| riia-jun-release/dashboard/js/fno/app-init.js | Add `_normScenarioLevels()` helper, normalization call, add `renderOverviewFromState` + `renderPortfolioHedgeRadar` to `_renderAll()`, update imports |
| riia-jun-release/dashboard/js/fno/payoff.js | Update `renderPayoffChart()` for portfolio-shape vs legacy NIFTY/BANKNIFTY shape |
| riia-jun-release/dashboard/js/fno/stress.js | Add `renderAnalyticsStress()` for state.stressData historical events |
| riia-jun-release/dashboard/js/fno/hedge.js | Add `renderPortfolioHedgeRadar()` for state.hedgeQuality.positions; export it |
| riia-jun-release/dashboard/js/fno/manoeuvre.js | Fix line ~140: p.instrument → p.full ?? p.instrument ?? p.und |
| riia-jun-release/dashboard/js/fno/greeks.js | Fix `renderGreeksTable()` to use g.und + g.hedge_type, g.ann_vol_pct instead of g.full, g.iv |
| riia-jun-release/dashboard/js/fno/main.js | Add `window.fnoSelectInstrument = fnoSelectInstrument`; import `renderOverviewFromState` if needed |
| riia-jun-release/dashboard/fno.html | Remove paper-mode-chk toggle block; add fno-overview-inst-selector, fno-overview-positions-grid divs; add stress-events-row below stress-row; add hqs-portfolio-tbody section in page-hedge |
| project-office/specs/Spec_RITA_App.md | Note F30 Phase 3 extends portfolio-analytics consumer chain; scenario normalization note |
| project-office/specs/Spec_JS_Code.md | Update FnO module reference: my-portfolio.js exports `renderOverviewFromState`; hedge.js exports `renderPortfolioHedgeRadar`; document `_normScenarioLevels` in app-init |

**Edge cases to handle:**
1. Empty positions array (state.positions = []): show fno-mp-empty placeholder; instrument selector still renders from state.marketData keys or ASML static default
2. state.scenarioLevels empty: `_normScenarioLevels({})` returns {}; `_hasScenLevels` guard returns false; "No scenario levels available" fallback renders
3. API error / network failure: all new render functions guard `if (!state.portfolioMeta) return`; `renderPortfolioHedgeRadar` guards `if (!state.hedgeQuality?.positions?.length)`
4. Instrument selector with no market data for selected instrument: fall back gracefully — filter state.positions by und === _selectedInstrument, show — for price KPI
5. state.payoffData has empty labels arrays: `renderPayoffChart()` already guards `if (!labels.length || !el)` — no chart renders, acceptable empty state
6. state.hedgeQuality.positions has instruments not in state.positions.und: `renderPortfolioHedgeRadar` renders from state.hedgeQuality.positions directly without cross-reference

**Definition of Done checklist:**
- [ ] API contract matches (schema fields = JS reads)
- [ ] Experience tier read-only (no new endpoint; existing endpoint has no db.commit())
- [ ] Section loaders registered in app-init.js `_renderAll()`
- [ ] Window bindings set (`window.fnoSelectInstrument` in main.js)
- [ ] Error handled in JS (try/catch, shows — on failure, null guards on all new state reads)
- [ ] Spec updated (both Spec_RITA_App.md and Spec_JS_Code.md)
- [ ] Ruff passes (ruff check src/ — no Python changes but verify no regressions)
- [ ] No hardcoded values (ASML as JS constant, not HTML string literal; no hardcoded lot sizes)

> Architect: complete ALL fields above before handing off to Engineer. Incomplete sections will be returned.

---

## [Reviewer] Design Review

**Mode:** Design Review  
**Status:** PASS  
**Reviewed:** [Architect] Design section  
**Against:** Requirements in ## Request

**Findings:**

| # | Finding | Severity | Rule cited | Resolution required |
|---|---|---|---|---|
| 1 | Equity hedge positions grid removal not designed. Requirement: "we do not require equity hedge positions grid again." No DOM element is listed under "DOM elements to REMOVE" for the equity hedge positions section, and no file change in files-to-touch covers its removal. Engineer must identify and remove the equity hedge positions DOM block from fno.html. | ADVISORY | Requirements coverage — reviewer.md §2 Mode A | Engineer: identify the equity hedge positions grid DOM ID in fno.html and add it to the removal list in my-portfolio.js or directly in fno.html |
| 2 | Closed positions panel removal not designed. Requirement: "we can remove closed positions." No DOM ID for a closed-positions panel appears in elements to REMOVE, and no file change addresses this. Engineer must identify and remove or hide the closed-positions panel. | ADVISORY | Requirements coverage — reviewer.md §2 Mode A | Engineer: identify the closed-positions DOM block in fno.html, add its ID to DOM elements to REMOVE, and remove it in the fno.html file change |
| 3 | Region/strategy/total allocation removal not explicitly designed. Requirement: "region allocation, strategy and total allocation is not needed." Feature summary mentions merging the portfolio widget but does not enumerate which DOM elements are removed. No corresponding removal entry exists in the files-to-touch table for these three allocation display elements. | ADVISORY | Requirements coverage — reviewer.md §2 Mode A | Engineer: identify the region allocation, strategy, and total allocation DOM blocks, confirm they are removed or excluded from `renderOverviewFromState()`, and document the DOM IDs removed |

**Checklist:**
- [x] Requirements coverage: PASS (with 3 advisory gaps — equity hedge grid, closed positions, allocation widgets; no design element is wrong, scope is clear enough for engineer to resolve)
- [x] API contract completeness: PASS — existing endpoint reused; method, path, query params, auth, and tier placement (`/api/v1/experience/`) all declared; response fields mapped to state fields with shapes and consuming pages
- [x] Frontend contract completeness: PASS — 8 JS modules named; DOM IDs listed per module; all state fields consumed by JS are listed in the state fields table with API response key and shape
- [x] Files-to-touch completeness: PASS — 11 files listed: 7 JS modules, 1 HTML file, 2 spec files; both frontend and spec files covered; no backend changes needed (no new endpoint)
- [x] Definition of Done populated: PASS — 8 specific, non-template items covering API contract, tier safety, section loader registration, window bindings, error handling, spec update, ruff, and hardcoded-values check

**Decision:** PASS — proceed to Engineer. Advisory findings 1–3 do not block implementation; the three omitted removal steps (equity hedge grid, closed positions panel, region/strategy/total allocation widgets) are unambiguous from the requirements and the engineer must include their DOM removal in the fno.html file change alongside the stated additions.

---

## [Engineer] Implementation Log

**Branch:** `worktree-agent-a3b56e29b2a337c63`

**Worktree path:** `/Users/sgawde/work/riia-jun-demo/riia-cowork-jun-demo/.claude/worktrees/agent-a3b56e29b2a337c63`

**Commit hash:** `f7c60e8`

**Files changed:**
| File | Change made |
|---|---|
| `dashboard/js/fno/app-init.js` | Added `_normScenarioLevels()` helper; applied to `data.scenario_levels`; added imports for `renderPortfolioHedgeRadar` and `renderOverviewFromState`; updated `_renderAll()` to call both |
| `dashboard/js/fno/my-portfolio.js` | Added `state` import; `_selectedInstrument='ASML'`; exported `renderOverviewFromState()` (instrument pills, KPI strip, filtered positions grid); exported `fnoSelectInstrument(id)` |
| `dashboard/js/fno/hedge.js` | Added exported `renderPortfolioHedgeRadar()` — KPI counts, alert banner, HQS table from `state.hedgeQuality.positions` |
| `dashboard/js/fno/payoff.js` | Added portfolio-mode branch in `renderPayoffChart()` — portfolio+hedged datasets, hides BANKNIFTY wrap; legacy path on fallthrough |
| `dashboard/js/fno/stress.js` | Added `renderAnalyticsStress()` for historical event table from `state.stressData`; called from `renderStressScenarios()` |
| `dashboard/js/fno/manoeuvre.js` | Fixed `manLots()` lotKey: `p.instrument` → `p.full ?? p.instrument ?? p.und` |
| `dashboard/js/fno/greeks.js` | Fixed `renderGreeksTable()`: `g.full` → `g.und`+`g.hedge_type`; `g.iv` → `g.ann_vol_pct ?? '—'`; null guards added |
| `dashboard/js/fno/main.js` | Added `fnoSelectInstrument` import; `window.fnoSelectInstrument` binding |
| `dashboard/fno.html` | Removed paper-mode-chk toggle; removed closed positions section; removed ASML equity hedge grid; removed region/strategy/total allocation panels; added `fno-overview-inst-selector`, 3-card KPI strip, `fno-overview-positions-grid`/`body`; added `stress-events-row` in page-risk |
| `project-office/specs/Spec_RITA_App.md` | Updated portfolio-analytics row with F30 Phase 3 consumer chain details |
| `project-office/specs/Spec_JS_Code.md` | Updated `app-init.js`, `hedge.js`, `my-portfolio.js` module entries |

**API contract verified:** yes — no new endpoint; all state field reads match Phase 1 PortfolioAnalyticsResponse schema fields

**Spec updated:** yes — both spec files grep-confirmed: `F30 Phase 3`, `renderOverviewFromState`, `renderPortfolioHedgeRadar`, `_normScenarioLevels` all present

**Ruff result:** passed — `All checks passed!`

**Definition of Done — Engineer check:**
- [x] API contract matches — all JS state field reads match published schema
- [x] Experience tier read-only — no new endpoint; existing endpoint unchanged
- [x] Section loader registered — `_renderAll()` calls `renderOverviewFromState()`, `renderPortfolioHedgeRadar()`; `renderAnalyticsStress()` from `renderStressScenarios()`
- [x] Window bindings set — `window.fnoSelectInstrument` in main.js
- [x] Error handled — try/catch in `renderOverviewFromState()`, `renderPortfolioHedgeRadar()`; early-return guards on all new renders
- [x] Spec updated — both Spec_RITA_App.md and Spec_JS_Code.md updated
- [x] Ruff passes — confirmed clean
- [x] No hardcoded values — `_selectedInstrument='ASML'` as JS constant; no lot sizes; no localhost URLs

---

## [Reviewer] Code Review

**Mode:** Code Review  
**Status:** {PASS | CONDITIONAL | FAIL}  
**Reviewed:** [Engineer] Implementation Log + changed files  
**Against:** [Architect] Design + engineer-role guardrails

**Findings:**

| # | File | Line | Finding | Severity | Rule cited |
|---|---|---|---|---|---|
| — | {file or "No findings"} | {line / N/A} | {description} | {BLOCKING / ADVISORY} | {ADR / guardrails/roles/engineer.md §N} |

**Checklist:**
- [ ] Implementation matches design (paths, fields, DOM targets): {PASS / FAIL}
- [ ] JS frontend contract verified: {PASS / FAIL / N/A}
- [ ] Engineer guardrails followed (worktree, no print, no hardcoded secrets/lot sizes): {PASS / FAIL}
- [ ] Spec updated: {PASS / FAIL / N/A}

**Decision:** {PASS — proceed to QA. | CONDITIONAL — proceed to QA; engineer addresses advisory items in follow-up. | FAIL — return to Engineer. Must fix: [list blocking findings].}

---

## [QA] Test Results

**Tests written:** {n}

**Test file:** `tests/{unit|integration}/{name}_test.py`

**Tests passed:** {n} / {n}

**Coverage delta:** {+/- n%}

**Contract check:**
| Schema field | Handler returns | Match? |
|---|---|---|
| {field} | {value/type} | {yes \| no} |

**Edge cases tested:**
- {list each edge case from Architect section — tested? yes/no}

---

## [TechWriter] Documentation

**Confluence page updated:** {URL | n/a — reason}

**Page section modified:** {section name on the Confluence page}

**Spec file confirmed current:** {yes | no}

**Task brief archived:** {yes}

---

*Brief generated by /enhance orchestrator. Each section written by the designated agent role.*
