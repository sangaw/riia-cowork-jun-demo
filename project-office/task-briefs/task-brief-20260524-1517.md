# Task Brief — 20260524-1517
**Run ID:** 20260524-1517
**Status:** in-progress

---

## Request
Wire up the functional-kpis.js panel to a live backend endpoint instead of the static JSON file it currently fetches. Implement GET /api/experience/ops/functional-kpis returning hourly KPI time-series for training success rate, chat low-confidence rate, experience error rate, overall error rate, and p95 latency.

## App Target
ops (existing `functional-kpis` section in ops.html)

## Skill Selected
`project-office/skills/skill-add-ops-feature.md`

## Spec Reference
- `project-office/specs/Spec_RITA_App.md`
- `project-office/specs/Spec_JS_Code.md`

---

## [PM] Validation

**Sprint alignment:** In scope. Ops dashboard data quality improvement.

**Risk flags:**
- LOW — No new DB tables. Reads from existing api_call_log and training_runs tables + CSV chat monitor.
- LOW — OperationalError fallback ensures endpoint never 500s on empty/missing tables.

**Dependencies:** None.

**Approved to proceed:** yes

---

## [Architect] Design

**Feature summary:** Replace the static JSON fetch in functional-kpis.js with a live GET /api/experience/ops/functional-kpis endpoint that aggregates hourly KPI buckets from existing data sources (api_call_log, training_runs, chat_monitor.csv).

**API contract:**
| Field | Value |
|---|---|
| Method | GET |
| Path | `/api/experience/ops/functional-kpis` |
| Query params | `hours: int = 24` (range 1–168) |
| Request body | none |
| Response shape | `FunctionalKPIsResponse` |
| Auth required | No |

**Response schema:**
```
FunctionalKPIsResponse {
  generated_at: str
  buckets: list[str]          # ISO hour labels e.g. "2026-05-24T14:00"
  series: FunctionalKPIsSeries {
    training_success_rate_pct: list[float]
    chat_low_confidence_pct: list[float]
    experience_error_pct: list[float]
    error_rate_pct: list[float]
    p95_latency_ms: list[float]
  }
}
```

**Frontend target:**
| Item | Value |
|---|---|
| JS module | `dashboard/js/ops/functional-kpis.js` (existing, modified) |
| HTML section | Already present in ops.html — no HTML changes needed |
| DOM element IDs | `functional-kpis-container` |
| Window bindings | n/a — `loadFunctionalKPIs()` registered in main.js already |

**Files to touch:**
| File | Change |
|---|---|
| `src/rita/schemas/functional_kpis.py` | NEW — FunctionalKPIsSeries, FunctionalKPIsResponse |
| `src/rita/api/experience/ops.py` | Add GET /functional-kpis endpoint |
| `dashboard/js/ops/functional-kpis.js` | Add `import { api }` + replace fetch with api() call |
| `project-office/specs/Spec_RITA_App.md` | Add endpoint row to Ops Experience Endpoints table |
| `project-office/specs/Spec_JS_Code.md` | Add module row to ops module structure table |

**Definition of Done checklist:**
- [x] API contract matches — Pydantic schema field names match JS `data.series[def.key]` reads
- [x] Experience tier read-only — no db.commit() in ops.py
- [x] `get_settings()` used, not bare `settings` — used inside `_read_chat_monitor_rows()`
- [x] Section loader registered — already registered in main.js (no change needed)
- [x] Window bindings set — already set in main.js (no change needed)
- [x] Error handled — try/catch in loadFunctionalKPIs(); OperationalError fallback in endpoint
- [x] Spec updated — endpoint + module rows added to both spec files
- [x] HTML changes — n/a (Architect listed zero HTML files to touch)
- [x] Ruff passes — all checks passed
- [x] No hardcoded values — no localhost URLs

---

## [Engineer] Implementation Log

**Branch:** `worktree-agent-a3cc9d0dc6edc42c6`

**Worktree path:** `/Users/sgawde/work/riia-jun-demo/riia-cowork-jun-demo/.claude/worktrees/agent-a3cc9d0dc6edc42c6`

**Commit hash:** `b16847d`

**Files changed:**
| File | Change made |
|---|---|
| `riia-jun-release/src/rita/schemas/functional_kpis.py` | NEW — FunctionalKPIsSeries, FunctionalKPIsResponse Pydantic schemas |
| `riia-jun-release/src/rita/api/experience/ops.py` | Added GET /functional-kpis endpoint; added csv/os/datetime imports; added FunctionalKPIsResponse schema import |
| `riia-jun-release/dashboard/js/ops/functional-kpis.js` | Added `import { api } from './api.js'`; replaced fetch('/ops/metrics/functional-kpis.json') with `await api('/api/experience/ops/functional-kpis')` |
| `project-office/specs/Spec_RITA_App.md` | Added functional-kpis endpoint row to Ops Experience Endpoints table |
| `project-office/specs/Spec_JS_Code.md` | Added functional-kpis.js module row to ops module structure table |
| `project-office/task-briefs/task-brief-20260524-1517.md` | Created this task brief |

**API contract verified:** yes — all JS `data.series[def.key]` reads (training_success_rate_pct, chat_low_confidence_pct, experience_error_pct, error_rate_pct, p95_latency_ms) match exact Pydantic field names in FunctionalKPIsSeries

**Spec updated:** yes — Spec_RITA_App.md (line 113) and Spec_JS_Code.md (line 94) both updated

**Ruff result:** passed

**Definition of Done — Engineer check:**
- [x] API contract matches — all JS field reads verified against Pydantic schema fields
- [x] Experience tier read-only — no db.commit() in the new route
- [x] `get_settings()` used, not bare `settings` — called inside `_read_chat_monitor_rows()` function body
- [x] Section loader registered — already in main.js (`_sectionLoaders['functional-kpis'] = loadFunctionalKPIs`); no change needed
- [x] Window bindings set — already in main.js; no new onclick handlers needed
- [x] Error handled — try/catch in JS loader shows "KPI data unavailable"; OperationalError handled; outer Exception returns all-zeros response
- [x] Spec updated — endpoint row in Spec_RITA_App.md + module row in Spec_JS_Code.md; grep confirmed both insertions
- [x] HTML changes — n/a (Architect files-to-touch listed zero HTML files)
- [x] Ruff passes — `ruff check src/` returned "All checks passed!"
- [x] No hardcoded values — no localhost URLs; uses api() which reads window.RITA_API_BASE

---

## [QA] Test Results

**Tests written:** {n}

**Test file:** `tests/{unit|integration}/{name}_test.py`

**Tests passed:** {n} / {n}

**Coverage delta:** {+/- n%}

---

## [TechWriter] Documentation

**Confluence page updated:** {URL | n/a}

**Spec file confirmed current:** yes

**Task brief archived:** yes

---

*Brief generated by /enhance orchestrator.*
