# Task Brief — 20260603-2019
**Run ID:** 20260603-2019
**Status:** engineer-complete

---

## Request
F29 Phase 3 — FnO Overview redesign

## App Target
fno

## Skill Selected
`project-office/skills/skill-add-fno-feature.md`

## Spec Reference
- `project-office/specs/Spec_RITA_App.md`
- `project-office/specs/Spec_JS_Code.md`

---

## [PM] Validation

**Sprint alignment:** In scope. F29 is the active feature as of 2026-06-03. Phase 3 (Overview redesign) is explicitly listed as "READY" in PLAN_STATUS.md and is the designated next step now that Phase 2 has been fully merged (commit `d96dd4c`). PLAN_STATUS.md step 4 for the next session reads: `"/enhance fno 'F29 Phase 3 — FnO Overview redesign'"`. The sprint critical path is: Phase 0 → Phase 1 → Phase 2 (all complete) → **Phase 3** → Phase 4 (spec consolidation). The prerequisite for Phase 3 per REQUIREMENTS.md is "Phase 1 (hedge-plan GET needed for hedge status card)" — Phase 1 is merged and live in the dev environment. Phase 3 is unblocked.

**Risk flags:**
- Medium risk: Phase 3 enriches `my-portfolio.js` to call three data sources in parallel: `geography-overview` (for 1Y return and risk score per instrument), `hedge-plan` (for `hedged_ids`), and `portfolio` (for `total_value_eur` and holdings). Parallel fetch error-handling must be robust — if any one of the three fails, the page must still render with available data; a single failed call must not blank the entire Overview. Architect must specify per-source fallback behaviour.
- Medium risk: The KPI strip "Portfolio Value" card must gracefully handle users whose `total_value_eur` is not set (null or zero). The spec says show `'—'` with a hint. The JS implementation must guard for this without crashing the chart or table render that follows.
- Medium risk: The allocation chart (Chart.js doughnut or stacked bar, grouped by region) requires a region map from instrument IDs to India/US/EU/Other. This data must come from `geography-overview` response or a known static mapping. If `geography-overview` returns unexpected region labels, the chart grouping will silently produce an "Other" bucket that misleads users. Architect must pin the region field name and expected values.
- Low risk: The "Hedged?" column in the holdings table reads from `hedged_ids` in the saved hedge plan. If the hedge plan's `hedged_ids` contains IDs that no longer exist in the current portfolio (stale plan), the column must silently show "—" without error. This was a known edge case handled in Phase 2; Phase 3 JS must apply the same guard.
- Low risk: The accumulated changeset (F28 UX + F29 Phases 0–2) is 54 commits ahead of `origin/master` per PLAN_STATUS.md. Phase 3 development is local-only and does not require prod deployment first; however, the prod deployment should happen before or immediately after Phase 3 merge to avoid an even larger undeployed delta.
- Low risk: Phase 3 modifies `fno.html` (Overview section) and `dashboard/js/fno/my-portfolio.js`. The existing Overview section in `fno.html` contains a "My Portfolio" table that must be replaced. Engineer must identify the exact DOM bounds of the current Overview section to avoid accidentally removing adjacent sections.

**Dependencies:**
- Prior run deliverables:
  - F29 Phase 0 (task-brief-20260603-1611, commit `b822859`, merge `b8a712e`): Removed `_DURATION_MONTHS` dict and `duration` Query param from `portfolio_hedge.py`; removed `phSetDuration` export from `portfolio-hedge.js` and its import/binding from `main.js`; hardcoded `t_months = 12.0` backend and `tMonths = 12` frontend; both spec files (`Spec_RITA_App.md`, `Spec_JS_Code.md`) updated; ruff passed.
  - F29 Phase 1 (task-brief-20260603-1640, commit `f1fcefa`, QA tests `f731e1f`, merge `e839dda`): Created `user_hedge_plans` ORM model + Alembic migration (`20260603_add_user_hedge_plans`); `UserHedgePlanRepo` (`find_by_key_id()` + `upsert()`); `HedgePlanCreate` / `HedgePlanOut` Pydantic schemas; Experience-tier `GET` + `PUT` at `/api/v1/experience/fno/hedge-plan`; `main.py` router registration; both spec files updated; ruff passed; 9 QA tests written and passing (commit `f731e1f`). Code Review: PASS. Fully merged to master.
  - F29 Phase 2 (task-brief-20260603-1752, commits `0bfbaeb` + `ce23a9f`, QA `8a836d3`, merge `d96dd4c`): `portfolio-hedge.js` wired to GET/PUT hedge-plan endpoints — `loadHedgePlan()` (restores `hedgeChecked`, `coverage`, `scenarioTab` on load; silent 404 fallback to defaults), `saveHedgePlan()` (fire-and-forget PUT, console.warn on failure), `_debouncedSave()` (400ms debounce called from `phToggleHedge`, `phSetCoverage`, `phSetScenarioTab`); bug fix: `_fetchHedge` loop no longer overwrites `hedgeChecked` after plan load; `Spec_JS_Code.md` updated with Phase 2 note. Code Review: PASS. 4 additional QA tests (total 13). Fully merged to master.
- Alembic migration `20260603_add_user_hedge_plans` must be confirmed applied in dev env — it was confirmed applied in Phase 2 testing; no new migration is expected for Phase 3.
- No external service dependencies.

**Cross-system scope check:**
- Primary target: `fno` dashboard (`fno.html` Overview section + `dashboard/js/fno/my-portfolio.js`). Phase 3 is a pure frontend redesign consuming pre-existing Experience-tier endpoints — no new backend code is expected.
- Does this feature affect the `rita` dashboard? No — the Overview redesign is FnO-only. The Portfolio Builder in `rita.html` is explicitly listed as "Out of Scope" in REQUIREMENTS.md Phase 3. No RITA dashboard JS modules are changed.
- Does this feature affect the `ops` dashboard? No — the ops dashboard has no connection to portfolio hedge plans, FnO Overview, or the `user_hedge_plans` table.
- Does this feature affect the mobile app (`mobileapp/index.html`)? No — the FnO Overview redesign targets the desktop FnO dashboard only. The mobile PWA has its own pages and is not in scope for this phase.

**Approved to proceed:** yes

> If no: state reason and stop. Do not spawn Architect agent.

---

## [Architect] Design

**Version:** v2 (corrected — addresses Design Review FAIL findings: instMap build step added; "(indicative)" label placements specified)

**Feature summary:** The FnO Overview redesign enriches the "My Portfolio" section inside `page-overview` so that authenticated users can see a live, data-rich snapshot of their saved portfolio at a glance — including a 5-card KPI strip, a region-allocation doughnut chart, a hedge status card, and a 6-column holdings table — all assembled from three parallel API calls with graceful per-source fallback. All computed/derived KPI values are labelled "(indicative)" per the platform-wide research-tool disclosure.

**API contracts consumed (3 existing endpoints, no new endpoint):**

*Endpoint 1 — Portfolio:*
| Field | Value |
|---|---|
| Method | GET |
| Path | `/api/v1/experience/user-portfolio` |
| Auth required | yes — JWT |
| Response fields read | `name`, `updated_at`, `total_value_eur` (float\|null), `holdings[]`, `holdings[].instrument_id`, `holdings[].allocation_pct` |

*Endpoint 2 — Geography Overview:*
| Field | Value |
|---|---|
| Method | GET |
| Path | `/api/experience/rita/geography-overview` |
| Auth required | no |
| Response fields read | `regions[]`, `regions[].region` (exact values: "India","US","EU","Other"), `regions[].instruments[]`, `regions[].instruments[].id`, `regions[].instruments[].return_1y_pct`, `regions[].instruments[].risk_score` |
| Key note | `region` is on the parent `regions[]` object (GeoRegion), NOT on `regions[].instruments[]`. The instMap build step must propagate it explicitly (see instMap build step below). |

*Endpoint 3 — Hedge Plan:*
| Field | Value |
|---|---|
| Method | GET |
| Path | `/api/v1/experience/fno/hedge-plan` |
| Auth required | yes — JWT |
| Response fields read | `hedged_ids`, `coverage`, `scenario_tab`, `updated_at` |
| Note | 404 = no plan (not an error); hedgePlan = null |

**Frontend target:**
| Item | Value |
|---|---|
| JS module | `dashboard/js/fno/my-portfolio.js` (full replacement) |
| Section id | `page-overview` (existing section; My Portfolio block inside it is replaced) |
| DOM element IDs | `fno-mp-name`, `fno-mp-updated`, `fno-mp-empty`, `fno-mp-error`, `fno-mp-kpi-value`, `fno-mp-kpi-holdings`, `fno-mp-kpi-return`, `fno-mp-kpi-risk`, `fno-mp-kpi-hedged`, `fno-mp-alloc-chart` (Chart.js canvas), `fno-mp-hedge-card`, `fno-mp-holdings-body` (tbody), `fno-mp-total` |
| Window bindings | `window.fnoMpGoHedge` (new — CTA navigates to portfolio-hedge section); `window.loadFnoMyPortfolio` (already exists) |

**Files to touch:**
| File | Change |
|---|---|
| `riia-jun-release/dashboard/js/fno/my-portfolio.js` | Full replacement: 3-source parallel fetch via Promise.allSettled + instMap build step + 5 render functions (_renderKpis, _renderAllocChart, _renderHedgeCard, _renderHoldingsTable, _renderCta) |
| `riia-jun-release/dashboard/fno.html` | Replace the My Portfolio HTML block (~lines 613–641) with new 5-component structure: KPI strip, 2-column row (alloc chart left + hedge card right), 6-column holdings table, CTA button |
| `riia-jun-release/dashboard/js/fno/main.js` | Add window.fnoMpGoHedge binding |
| `project-office/specs/Spec_JS_Code.md` | Update my-portfolio.js row with Phase 3 note and new DOM IDs |
| `project-office/specs/Spec_HTML_Code.md` | Update My Portfolio block description with new component list and DOM element IDs |

**instMap build step (BLOCKING FIX — explicit region propagation from parent):**

`region` lives on the parent `regions[]` object, not on `regions[].instruments[]`. The build loop must propagate it into each instrument entry. Engineer must use this exact pattern:

```js
const instMap = {};
for (const reg of (geoData?.regions ?? [])) {
  for (const inst of (reg.instruments ?? [])) {
    instMap[inst.id] = {
      return_1y_pct: inst.return_1y_pct,
      risk_score:    inst.risk_score,
      region:        reg.region   // propagated from parent GeoRegion object
    };
  }
}
```

If `geoData` is null/unavailable (geo fetch failed), `instMap` remains `{}` and all geo-derived fields degrade gracefully.

**Parallel fetch strategy:**

`Promise.allSettled([portfolio, geo, hedgePlan])` with per-source fallback:
- portfolio 404 → show `fno-mp-empty`; all other components hidden
- geo fail → `instMap = {}`; geo-derived fields degrade to "—"; chart shows "Other: 100%"
- hedge-plan 404 → `hedgePlan = null`; hedge card shows "No hedge configured"

**Key computation details:**

Wtd 1Y Return formula:
```js
const validHoldings = holdings.filter(h => instMap[h.instrument_id]?.return_1y_pct != null);
const totalValidPct = validHoldings.reduce((s, h) => s + h.allocation_pct, 0) || 1;
const wtdReturn = validHoldings.reduce((s, h) =>
  s + (h.allocation_pct / totalValidPct) * instMap[h.instrument_id].return_1y_pct, 0);
```

Avg Risk formula:
```js
const validRisk = holdings.filter(h => instMap[h.instrument_id]?.risk_score != null);
const totalRiskPct = validRisk.reduce((s, h) => s + h.allocation_pct, 0) || 1;
const avgRisk = validRisk.reduce((s, h) =>
  s + (h.allocation_pct / totalRiskPct) * instMap[h.instrument_id].risk_score, 0);
```

Region bucket computation (now correct because instMap carries `region` from the build step):
```js
const regionBuckets = { India: 0, US: 0, EU: 0, Other: 0 };
for (const h of holdings) {
  const region = instMap[h.instrument_id]?.region ?? 'Other';
  regionBuckets[region] = (regionBuckets[region] || 0) + h.allocation_pct;
}
```

Hedged? column: `hedgedIdsSet.has(h.instrument_id)` → strategy label from `{pp:'Protective Put', ps:'Put Spread', collar:'Collar'}[scenario_tab]` or "—"; stale IDs silently skipped (table iterates holdings, not hedged_ids)

CTA navigation: `window.fnoMpGoHedge` removes active from all nav-items/sections, activates portfolio-hedge nav item and section, calls `_sectionLoaders['portfolio-hedge']()`

**"(indicative)" label placements (all required):**

| Location | Element | Label text |
|---|---|---|
| KPI strip — Wtd 1Y Return card subtitle | subtitle text node inside `fno-mp-kpi-return` | "weighted avg (indicative)" |
| KPI strip — Avg Risk card subtitle | subtitle text node inside `fno-mp-kpi-risk` | "weighted avg (indicative)" |
| KPI strip — Hedge Coverage card subtitle | subtitle text node inside `fno-mp-kpi-hedged` | "from saved plan (indicative)" |
| Hedge Status card — bottom note | note `<p>` inside `fno-mp-hedge-card` | "Values are indicative — not financial advice" (unconditional; always shown) |
| Holdings table header — 1Y Return column | `<th>` for return column | "1Y Return (ind.)" |
| Holdings table header — Risk column | `<th>` for risk column | "Risk (ind.)" |

**Edge cases to handle (7):**
1. hedge-plan 404 → `hedgePlan = null`; hedge card shows "No hedge configured"; CTA shows "Configure Hedge →"; Hedged? column shows "—" for all rows
2. `total_value_eur` null/0 → KPI shows "—" with subtitle "Not set — add in Portfolio Builder"; Position EUR column shows "—"; chart and table still render
3. `hedged_ids` contains stale IDs not in current portfolio → silently skipped (table iterates holdings, not hedged_ids)
4. portfolio 404 → show `fno-mp-empty`; all other components hidden
5. geo fails or returns empty → `instMap = {}`; all geo-derived fields degrade to "—"; chart shows "Other: 100%" single slice
6. instMap has instruments not in portfolio → no error (table loop iterates holdings only; unused instMap entries are never read)
7. all `allocation_pct = 0` → `totalValidPct` guard (`|| 1`) prevents division by zero; displays 0.0% not NaN

**Definition of Done checklist (10 items):**
- [ ] API contract matches — all field names verified; `region` confirmed as `GeoRegion.region` (parent level, not instrument level)
- [ ] instMap build step present and correct — loops `regions[]` then `reg.instruments[]`; sets `instMap[inst.id].region = reg.region`
- [ ] Correct tier used — Experience-tier reads only; FC-TIER grep empty
- [ ] Section loader registered — `loadFnoMyPortfolio` already in main.js; `fnoMpGoHedge` window binding added
- [ ] Window bindings set — `window.fnoMpGoHedge` added to `fno/main.js`
- [ ] Error handled — Promise.allSettled with per-source fallback; empty state; "—" on all missing data
- [ ] "(indicative)" labels present — KPI strip subtitles for Wtd 1Y Return, Avg Risk, Hedge Coverage; Hedge Status card note; holdings table headers for 1Y Return and Risk
- [ ] Spec updated — `Spec_JS_Code.md` + `Spec_HTML_Code.md` updated and confirmed via grep
- [ ] HTML changes complete — `fno.html` block replaced via Grep → ±15 line read → targeted Edit
- [ ] FC-IMP gate passed — `apiFetch` from `./api.js`; `_sectionLoaders` from `./nav.js`; `mkChart`, `C` from `../shared/charts.js`; `setEl` from `../shared/utils.js`; no direct `new Chart()` calls

> Architect: complete ALL fields above before handing off to Engineer. Incomplete sections will be returned.

---

## [Reviewer] Design Review

**Mode:** Design Review (second pass — re-review after Architect corrections)
**Status:** PASS
**Reviewed:** [Architect] Design section v2
**Against:** Requirements in ## Request + prior FAIL findings (instMap build step, "(indicative)" label placements)

**Findings:**

| # | Finding | Severity | Rule cited | Resolution required |
|---|---|---|---|---|
| — | No findings | — | — | — |

**Prior FAIL findings resolved:**
- BLOCKING (instMap build step): v2 adds an explicit nested loop in a dedicated "instMap build step" sub-section. The loop correctly iterates `regions[]` then `reg.instruments[]` and sets `instMap[inst.id].region = reg.region`, propagating the region from the parent GeoRegion object. The Key note on Endpoint 2 also calls this out explicitly. Finding resolved.
- ADVISORY ("(indicative)" labels): v2 adds a 6-row table specifying the exact DOM element, location, and label text for every occurrence (2 KPI subtitles, 1 hedge card note, 2 table headers). Finding resolved.

**Checklist:**
- [x] Requirements coverage: PASS — all 4 PM risk flags addressed (parallel fetch fallback strategy, null/zero total_value_eur guard, stale hedged_ids guard, region field pinned with exact values); both prior FAIL findings corrected in v2.
- [x] API contract completeness: PASS — all 3 endpoints have method, path, ≥2 response fields, and tier declared; all 3 are Experience-tier (compliant with ADR-001 / project.md §1); `/api/experience/rita/geography-overview` uses the allowed "ops prefix variant" listed in project.md §1.
- [x] Frontend contract completeness: PASS — JS module named (`dashboard/js/fno/my-portfolio.js`), 13 DOM IDs listed, 2 window bindings specified (`window.fnoMpGoHedge`, `window.loadFnoMyPortfolio`); all response fields read by the JS are present in the declared endpoint response shapes.
- [x] Files-to-touch completeness: PASS — 5 files listed covering frontend JS (my-portfolio.js, main.js), frontend HTML (fno.html), and both spec files (Spec_JS_Code.md, Spec_HTML_Code.md); no backend files expected per PM scope check ("pure frontend redesign consuming pre-existing Experience-tier endpoints").
- [x] Definition of Done populated: PASS — 10 specific, non-placeholder items; includes instMap verification, FC-TIER grep, "(indicative)" label presence, spec update confirmation, FC-IMP gate (import sources pinned).

**Decision:** PASS — proceed to Engineer.

---

## [Engineer] Implementation Log

**Branch:** `worktree-agent-a04116c73f950916f`

**Worktree path:** `/Users/sgawde/work/riia-jun-demo/riia-cowork-jun-demo/.claude/worktrees/agent-a04116c73f950916f`

**Commit hash:** `3cc9d04`

**Files changed:**
| File | Change made |
|---|---|
| `riia-jun-release/dashboard/js/fno/my-portfolio.js` | Full replacement — 3-source Promise.allSettled fetch; instMap build with explicit nested region loop; `_renderKpis`, `_renderAllocChart`, `_renderHedgeCard`, `_renderHoldingsTable`; per-source fallback; all "(indicative)" labels; correct `apiFetch(url, {headers})` usage |
| `riia-jun-release/dashboard/fno.html` | Targeted Edit replacing old 2-column table block with: empty/error placeholders, 5-card KPI strip, 2-col row (alloc chart + hedge card), 6-col holdings table with thead "1Y Return (ind.)" + "Risk (ind.)" headers, CTA button |
| `riia-jun-release/dashboard/js/fno/main.js` | Added `window.fnoMpGoHedge` binding — removes active classes from all nav-items/sections, activates portfolio-hedge nav item and section, calls `_sectionLoaders['portfolio-hedge']()` |
| `project-office/specs/Spec_JS_Code.md` | Updated FnO my-portfolio.js row with Phase 3 description, all DOM IDs, and window bindings |
| `project-office/specs/Spec_HTML_Code.md` | Updated My Portfolio block description with Phase 3 component list, all 13 DOM element IDs, indicative label placements |

**API contract verified:** yes — all 3 endpoints verified: `apiFetch` signature `(url, options={})` confirmed from `shared/api.js`; JWT passed via `{ headers: { Authorization: 'Bearer {token}' } }`; `apiFetch` returns `data|null` (not a `{ok,status,data}` wrapper). FC-004: all field accesses verified against Architect spec — `portfolio.{name, updated_at, total_value_eur, holdings}`, `holdings[].{instrument_id, allocation_pct}`, `reg.region`, `inst.{return_1y_pct, risk_score, id}`, `hedgePlan.{hedged_ids, coverage, scenario_tab, updated_at}`.

**Spec updated:** yes — both `Spec_JS_Code.md` and `Spec_HTML_Code.md` updated in worktree at `project-office/specs/`

**Ruff result:** passed — `All checks passed!` (no Python files changed)

**FC-IMP gate:**
- `apiFetch` from `./api.js` — `fno/api.js` re-exports from `../shared/api.js` which exports `apiFetch` ✓
- `mkChart`, `C` from `../shared/charts.js` — path resolves to `dashboard/js/shared/charts.js`; exports `mkChart` (line 10) and `C` (line 33) ✓
- `setEl` from `../shared/utils.js` — path resolves to `dashboard/js/shared/utils.js`; exports `setEl` (line 15) ✓
- `_sectionLoaders` from `./nav.js` — `fno/nav.js` exports `_sectionLoaders` (line 5) ✓

**FC-TIER gate:** no banned system-tier calls in `my-portfolio.js` — only Experience-tier paths used

**FC-MERGE gate:** no conflict markers in `my-portfolio.js` or `main.js`

**Definition of Done — Engineer check:**
- [x] API contract matches — all field names verified; `region` confirmed as `GeoRegion.region` (parent level, propagated via explicit nested loop)
- [x] instMap build step present and correct — loops `regions[]` then `reg.instruments[]`; sets `instMap[inst.id].region = reg.region`
- [x] Correct tier used — Experience-tier reads only; FC-TIER gate passed
- [x] Section loader registered — `loadFnoMyPortfolio` already in main.js; `fnoMpGoHedge` window binding added
- [x] Window bindings set — `window.fnoMpGoHedge` added to `fno/main.js`
- [x] Error handled — Promise.allSettled with per-source fallback; empty state; "—" on all missing data
- [x] "(indicative)" labels present — KPI strip subtitles for Wtd 1Y Return, Avg Risk, Hedge Coverage; Hedge Status card note "Values are indicative — not financial advice"; holdings table headers "1Y Return (ind.)" and "Risk (ind.)"
- [x] Spec updated — `Spec_JS_Code.md` + `Spec_HTML_Code.md` updated in worktree
- [x] HTML changes complete — `fno.html` block replaced via Grep (line 613) → ±20 line read → targeted Edit
- [x] FC-IMP gate passed — `apiFetch` from `./api.js`; `_sectionLoaders` from `./nav.js`; `mkChart`, `C` from `../shared/charts.js`; `setEl` from `../shared/utils.js`; no direct `new Chart()` calls

---

## [Reviewer] Code Review

**Mode:** Code Review  
**Status:** {PASS | CONDITIONAL | FAIL}  
**Reviewed:** [Engineer] Implementation Log + changed files  
**Against:** [Architect] Design + engineer-role guardrails

**Findings:**

| # | File | Line | Finding | Severity | Rule cited |
|---|---|---|---|---|---|
| — | {file or "No findings"} | {line / N/A} | {description} | {BLOCKING / ADVISORY} | {ADR / guardrails/roles/engineer.md §N} |

**Checklist:**
- [ ] Implementation matches design (paths, fields, DOM targets): {PASS / FAIL}
- [ ] JS frontend contract verified: {PASS / FAIL / N/A}
- [ ] Engineer guardrails followed (worktree, no print, no hardcoded secrets/lot sizes): {PASS / FAIL}
- [ ] Spec updated: {PASS / FAIL / N/A}

**Decision:** {PASS — proceed to QA. | CONDITIONAL — proceed to QA; engineer addresses advisory items in follow-up. | FAIL — return to Engineer. Must fix: [list blocking findings].}

---

## [QA] Test Results

**Tests written:** {n}

**Test file:** `tests/unit/{name}_test.py`

**Tests passed:** {n} / {n}

**Coverage delta:** {+/- n%}

**Contract check:**
| Schema field | Handler returns | Match? |
|---|---|---|
| {field} | {value/type} | {yes \| no} |

**Edge cases tested:**
- {list each edge case from Architect section — tested? yes/no}

---

## [TechWriter] Documentation

**Confluence page updated:** {URL | n/a — reason}

**Page section modified:** {section name on the Confluence page}

**Spec file confirmed current:** {yes | no}

**Task brief archived:** {yes}

---

*Brief generated by /enhance orchestrator. Each section written by the designated agent role.*
