# Task Brief — 20260603-1611
**Run ID:** 20260603-1611
**Status:** in-progress

---

## Request
F29 Phase 0 — Duration cleanup: remove 1m/3m pickers, hardcode 1y everywhere

## App Target
fno

## Skill Selected
`project-office/skills/skill-add-fno-feature.md`

## Spec Reference
- `project-office/specs/Spec_RITA_App.md`
- `project-office/specs/Spec_JS_Code.md`

---

## [PM] Validation

**Sprint alignment:** In scope. F29 is the active feature as of 2026-06-03. Phase 0 (duration cleanup — lock to 1y, remove 1m/3m pickers) is explicitly listed as READY in PLAN_STATUS.md and is the designated first step before Phase 1 (user_hedge_plans). This is a direct prerequisite to the rest of the F29 work.

**Risk flags:**
- Low risk overall — this is a UI simplification and backend default-param change with no new schema, no DB migration, and no new API surface.
- The duration parameter removal must be checked in all callers: `portfolio-hedge.js` (frontend), `portfolio_hedge.py` (backend default), and `fno.html` (any inline pickers or buttons referencing 1m/3m). Missing even one call site would leave inconsistent state.
- The `phSetDuration` window binding (if it exists) must be explicitly removed or confirmed absent; an orphaned window function is a silent API surface that could confuse future engineers.

**Dependencies:**
- No prior F29 task brief exists in `project-office/task-briefs/` — this is confirmed the first run for this feature.
- F28 UX fixes (layout reorder, Position € column, BANKNIFTY compactness, EUR save fix) are committed but not yet deployed to prod per PLAN_STATUS.md. Phase 0 does not depend on prod deployment of F28; local development can proceed. However, the deploy should happen before Phase 1 to avoid stacking undeployed changes.
- No external dependencies (no new DB migration, no new API endpoint, no third-party service).

**Cross-system scope check:**
- Primary target: `fno` dashboard (`fno.html` + `dashboard/js/fno/portfolio-hedge.js`).
- Does this feature affect `rita` dashboard? No — duration is an FnO-only concept; no duration picker or FnO hedge calls exist in `dashboard/js/rita/`.
- Does this feature affect `ops` dashboard? No — ops has no connection to portfolio hedge or FnO duration parameters.
- Does this feature affect `ds` (data science) dashboard? No — duration cleanup is confined to the FnO experience layer and its frontend module.

**Approved to proceed:** yes

> If no: state reason and stop. Do not spawn Architect agent.

---

## [Architect] Design

**Feature summary:** Phase 0 removes the 1m/3m duration options from the Portfolio Hedge surface and locks all hedge calculations to a 1-year tenor. Users no longer see a duration picker; the hedge plan is always computed on a 12-month horizon, simplifying the UI and eliminating a source of inconsistency before Phase 1 (user_hedge_plans) is built.

**API contract:**
| Field | Value |
|---|---|
| Method | GET |
| Path | `/api/v1/experience/fno/portfolio-hedge` |
| Change being made | Remove the `duration` query parameter entirely. Hardcode `t_months = 12.0` inside the handler. Remove `pattern="^(1m\|3m\|1y)$"` Query validator. The `duration` field remains in response always `"1y"`. |
| Query params | `coverage: int` (unchanged), `total_value_eur: float \| None` (unchanged). `duration` param is removed. |
| Request body | none |
| Response shape | unchanged — `duration` field still present in response, always `"1y"` |
| Auth required | yes — JWT |

**Frontend target:**
| Item | Value |
|---|---|
| JS module | `dashboard/js/fno/portfolio-hedge.js` |
| Section id | `page-portfolio-hedge` (existing section; no new section) |
| DOM element IDs to REMOVE | None — fno.html contains no duration picker buttons |
| Window bindings to REMOVE | `window.phSetDuration` — on line 95 of `dashboard/js/fno/main.js` |

The `phSetDuration` function export on line 522 of `portfolio-hedge.js` and its named import on line 86 of `main.js` must also be removed.

**Files to touch:**
| File | Change |
|---|---|
| `riia-jun-release/dashboard/js/fno/portfolio-hedge.js` | Remove `_DURATION_MONTHS` constant; remove `duration` from `_state`; hardcode `tMonths = 12` in `_renderDiscover()`; drop `&duration=...` from `_fetchHedge()` URL; remove `phSetDuration` export |
| `riia-jun-release/dashboard/js/fno/main.js` | Remove `phSetDuration` from named import; remove `window.phSetDuration` binding |
| `riia-jun-release/src/rita/api/experience/portfolio_hedge.py` | Remove `_DURATION_MONTHS` dict; remove `duration` Query param from handler; replace `t_months = _DURATION_MONTHS.get(duration, 12.0)` with `t_months = 12.0` |
| `riia-jun-release/dashboard/fno.html` | Verify no duration picker elements; no change expected but Engineer must grep-confirm |
| `project-office/specs/Spec_RITA_App.md` | Update portfolio-hedge row: remove duration param, note hardcoded to 1y |
| `project-office/specs/Spec_JS_Code.md` | Update portfolio-hedge.js row: remove duration from state/bindings/API line |

**Edge cases to handle:**
1. `phSetDuration` called from HTML onclick or other JS — Engineer must grep for residual callsites before committing
2. Stale `_state.duration` references — every `_state.duration` access must be replaced; if missed will return `undefined` and corrupt URL params silently
3. Backend receives legacy `duration` query param from old cached frontend — FastAPI ignores unknown query params by default; safe; Engineer should verify no `extra='forbid'`
4. `_DURATION_MONTHS` referenced after removal — Engineer must grep for zero remaining references after edit

**Definition of Done checklist:**
- [ ] Duration picker removed from Portfolio Hedge UI
- [ ] All FnO API calls use duration=1y
- [ ] phSetDuration removed or confirmed no callers
- [ ] fno.html has no remaining 1m/3m elements
- [ ] portfolio_hedge.py default param is 1y
- [ ] Ruff passes (ruff check src/)
- [ ] No hardcoded values (no localhost, no hardcoded lot sizes)
- [ ] Spec updated if any endpoint signature changed

---

## [Reviewer] Design Review

**Mode:** Design Review  
**Status:** PASS  
**Reviewed:** [Architect] Design section  
**Against:** Requirements in ## Request

**Findings:**

| # | Finding | Severity | Rule cited | Resolution required |
|---|---|---|---|---|
| — | No findings | — | — | — |

**Checklist:**
- [x] Requirements coverage: PASS — all three stated requirements (remove 1m/3m pickers, hardcode 1y in backend, confirm no picker DOM elements) have explicit design elements mapped to files and line references.
- [x] API contract completeness: PASS — no new endpoint is intentional and correct for a parameter-removal task. Existing endpoint `/api/v1/experience/fno/portfolio-hedge` (GET) is at the correct Experience tier per project.md §1. Change described precisely: `duration` Query param removed, `t_months` hardcoded to 12.0, response shape unchanged with `duration` field always `"1y"`. Auth requirement stated. Response fields ≥2 (duration + all existing fields unchanged).
- [x] Frontend contract completeness: PASS — JS module named (`portfolio-hedge.js`, `main.js`), DOM IDs to remove explicitly stated as "None" with justification, window binding to remove identified (`window.phSetDuration` line 95 of `main.js`), named import removal identified (line 86 of `main.js`), export removal identified (line 522 of `portfolio-hedge.js`). All response fields the JS reads are unchanged.
- [x] Files-to-touch completeness: PASS — 6 files listed covering backend (`portfolio_hedge.py`), frontend JS (`portfolio-hedge.js`, `main.js`), HTML verification (`fno.html`), and both spec files (`Spec_RITA_App.md`, `Spec_JS_Code.md`). Exceeds the ≥3 file threshold.
- [x] Definition of Done populated: PASS — 8 real, actionable items covering UI removal, API call correctness, binding removal, HTML verification, backend default, linting, hardcoded-value guard, and spec update. No template placeholders remain.

**Decision:** PASS — proceed to Engineer.

---

## [Engineer] Implementation Log

**Branch:** `worktree-agent-a8575b0dd6b8ce896`

**Worktree path:** `/Users/sgawde/work/riia-jun-demo/riia-cowork-jun-demo/.claude/worktrees/agent-a8575b0dd6b8ce896`

**Commit hash:** `b822859`

**Files changed:**
| File | Change made |
|---|---|
| `riia-jun-release/src/rita/api/experience/portfolio_hedge.py` | Removed `_DURATION_MONTHS` dict; removed `duration` Query param from handler; replaced `t_months = _DURATION_MONTHS.get(duration, 12.0)` with `t_months = 12.0`; hardcoded `duration="1y"` in `HedgeHolding` and `PortfolioHedgeResponse` return; updated docstring |
| `riia-jun-release/dashboard/js/fno/portfolio-hedge.js` | Removed `_DURATION_MONTHS` and `_DURATION_LABEL` constants; removed `duration: '1y'` from `_state`; hardcoded `tMonths = 12` in `_renderDiscover()` and `_renderAllocation()`; removed `&duration=${_state.duration}` from `_fetchHedge()` URL; removed `_state.duration = '1y'` from reset block; removed `phSetDuration` export function; updated API comment |
| `riia-jun-release/dashboard/js/fno/main.js` | Removed `phSetDuration` from named import; removed `window.phSetDuration = phSetDuration` binding |
| `project-office/specs/Spec_RITA_App.md` | Updated portfolio-hedge row: removed `duration` param, noted hardcoded to 1y, updated wizard tab count to 2 |
| `project-office/specs/Spec_JS_Code.md` | Updated portfolio-hedge.js row: removed `duration` from state, removed `phSetDuration(d)` from bindings, updated API URL, updated tab description |
| `project-office/task-briefs/task-brief-20260603-1611.md` | Added this implementation log |

**API contract verified:** yes — `GET /api/v1/experience/fno/portfolio-hedge?coverage=N` matches Architect design; `duration` field still present in response always `"1y"`

**Spec updated:** yes — both `Spec_RITA_App.md` and `Spec_JS_Code.md` updated

**Ruff result:** passed — `All checks passed!`

**Definition of Done — Engineer check:**
- [x] Duration picker removed from Portfolio Hedge UI — `phSetDuration` export and `_state.duration` removed
- [x] All FnO API calls use duration=1y — URL no longer sends `&duration=`; backend hardcodes `t_months = 12.0`; response `duration` field always `"1y"`
- [x] phSetDuration removed or confirmed no callers — removed from portfolio-hedge.js export, main.js import, and main.js window binding; fno.html confirmed clean (no picker elements)
- [x] fno.html has no remaining 1m/3m elements — grep confirmed zero results
- [x] portfolio_hedge.py default param is 1y — `t_months = 12.0` hardcoded; `_DURATION_MONTHS` dict and `duration` Query param removed
- [x] Ruff passes — `All checks passed!`
- [x] No hardcoded values — no localhost URLs, no hardcoded lot sizes; `t_months = 12.0` is the intentional F29 Phase 0 hardcode per spec
- [x] Spec updated if any endpoint signature changed — yes, both spec files updated

---

## [Reviewer] Code Review

**Mode:** Code Review  
**Status:** {PASS | CONDITIONAL | FAIL}  
**Reviewed:** [Engineer] Implementation Log + changed files  
**Against:** [Architect] Design + engineer-role guardrails

**Findings:**

| # | File | Line | Finding | Severity | Rule cited |
|---|---|---|---|---|---|
| — | {file or "No findings"} | {line / N/A} | {description} | {BLOCKING / ADVISORY} | {ADR / guardrails/roles/engineer.md §N} |

**Checklist:**
- [ ] Implementation matches design (paths, fields, DOM targets): {PASS / FAIL}
- [ ] JS frontend contract verified: {PASS / FAIL / N/A}
- [ ] Engineer guardrails followed (worktree, no print, no hardcoded secrets/lot sizes): {PASS / FAIL}
- [ ] Spec updated: {PASS / FAIL / N/A}

**Decision:** {PASS — proceed to QA. | CONDITIONAL — proceed to QA; engineer addresses advisory items in follow-up. | FAIL — return to Engineer. Must fix: [list blocking findings].}

---

## [QA] Test Results

**Tests written:** {n}

**Test file:** `tests/unit/{name}_test.py`

**Tests passed:** {n} / {n}

**Coverage delta:** {+/- n%}

**Contract check:**
| Schema field | Handler returns | Match? |
|---|---|---|
| {field} | {value/type} | {yes \| no} |

**Edge cases tested:**
- {list each edge case from Architect section — tested? yes/no}

**Definition of Done — QA check:**
- [ ] At least 1 unit test per new endpoint
- [ ] All tests pass
- [ ] API-frontend contract verified (no field name mismatches)
- [ ] Edge cases from Architect section covered

---

## [TechWriter] Documentation

**Confluence page updated:** {URL | n/a — reason}

**Page section modified:** {section name on the Confluence page}

**Spec file confirmed current:** {yes | no — if no, update before closing}

**Task brief archived:** yes — task-brief-20260603-1611.md

**Definition of Done — TechWriter check:**
- [ ] Confluence page reflects new feature
- [ ] Spec_RITA_App.md endpoint inventory is accurate
- [ ] Spec_JS_Code.md module structure table is accurate
- [ ] Task brief saved as completed artifact

---

*Brief generated by /enhance orchestrator. Each section written by the designated agent role.*
