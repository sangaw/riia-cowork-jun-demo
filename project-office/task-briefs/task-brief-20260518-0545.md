# Task Brief — 20260518-0545
**Run ID:** 20260518-0545
**Status:** complete

---

## Request
Feature 09 continuation run — QA + TechWriter for Run A backend (already merged to master at 872aeaf).
Run A complete: GET /api/v1/instrument/search + POST /api/v1/instrument/onboard live on master.
This run: QA tests (mock yfinance, 400/409/502 edge cases) + TechWriter (Confluence + spec confirmation).
Run B (daily-ops.js UI + ops.html panel) is a separate future /enhance run.

## App Target
ops

## Skill Selected
`project-office/skills/skill-add-ops-feature.md`

## Spec Reference
- `project-office/specs/Spec_RITA_App.md`
- `project-office/specs/Spec_JS_Code.md`

---

## [PM] Validation

**Sprint alignment:** In scope — Feature 09 is the current sprint feature; Run A backend is merged; this session completes QA + TechWriter for Run A before beginning Run B frontend.

**Risk flags:**
- yfinance is a live network dependency; QA must mock all yfinance calls to avoid flaky tests
- Alembic migration already applied (e9f3b2c41a07 → 20260517_add_currency); no re-run needed

**Dependencies:** Run A already complete (872aeaf on master). No unmet blockers.

**Approved to proceed:** yes

---

## [Architect] Design

**Note:** This is a continuation run. Engineer phase (Run A) is pre-completed on master at 872aeaf.
The design below documents the existing implementation for QA reference.

**Feature summary:** Adds a self-service instrument onboarding pipeline — company-name search via Yahoo Finance, automated OHLCV data fetch, normalization, DB seed, and instruments table registration. Two endpoints: search and onboard.

**API contract — GET /api/v1/instrument/search:**
| Field | Value |
|---|---|
| Method | GET |
| Path | `/api/v1/instrument/search` |
| Query params | `q: str` (required, min length 2) |
| Request body | none |
| Response shape | `list[{ticker: str, name: str, exchange: str, currency: str, country: str, quote_type: str}]` |
| Auth required | no |

**API contract — POST /api/v1/instrument/onboard:**
| Field | Value |
|---|---|
| Method | POST |
| Path | `/api/v1/instrument/onboard` |
| Query params | none |
| Request body | `{ticker: str, name: str, exchange: str, currency: str, country_code: str, lot_size: int \| null}` |
| Response shape | `{status: str, ticker: str, rows_fetched: int, rows_seeded: int, raw_path: str, input_path: str}` |
| Auth required | no |

**Frontend target:** (Run B — deferred)
| Item | Value |
|---|---|
| JS module | `dashboard/js/ops/daily-ops.js` (extension) |
| Section id | existing instruments section in ops.html |
| DOM element IDs | `dops-add-instrument-panel`, `dops-search-input`, `dops-search-results`, `dops-confirm-form`, `dops-onboard-status` |
| Window bindings | `searchInstruments`, `selectSearchResult`, `onboardInstrument`, `cancelOnboard` |

**Files touched (Run A — pre-completed):**
| File | Change |
|---|---|
| `pyproject.toml` | Added yfinance dependency |
| `src/rita/core/data_loader.py` | Renamed `load_nifty_csv` → `load_ohlcv_csv`; alias kept |
| `src/rita/core/data_understanding.py` | Made `find_instrument_csv()` dynamic (glob-based); NIFTY/BANKNIFTY exceptions preserved |
| `src/rita/models/instrument.py` | Added `currency` column |
| `alembic/versions/20260517_add_currency.py` | New migration: `op.add_column("instruments", currency)` |
| `src/rita/api/v1/system/instruments.py` | Added `currency` to `_InstrumentBody` and list response |
| `src/rita/services/instrument_onboard.py` | New service: `search_tickers()`, `fetch_raw_data()`, `process_to_input()`, `seed_market_cache()` |
| `src/rita/api/v1/workflow/instrument_onboard.py` | New router: GET /search + POST /onboard |
| `main.py` | Registered `instrument_onboard_router` |
| 10 call-site files | Updated `load_nifty_csv` imports → `load_ohlcv_csv` |
| `project-office/specs/Spec_RITA_App.md` | Added both endpoints to Workflow Tier table |
| `project-office/specs/Spec_Python_Code.md` | Added service + router to relevant tables |

**Edge cases to handle (for QA):**
- `q` param blank or length < 2 → 400
- yfinance `Search()` raises exception → 502
- `POST /onboard` with ticker already in instruments table → 409
- yfinance `download()` returns fewer than 100 rows → 400/502
- `POST /onboard` with valid ticker but yfinance network failure → 502

**Definition of Done checklist (Run A context):**
- [x] API contract matches (schema fields = handler return)
- [x] Workflow tier (no JWT, no experience layer required for onboard)
- [x] Router registered in main.py
- [x] Window bindings (deferred to Run B)
- [x] Ruff passes
- [x] Spec updated (Spec_RITA_App.md + Spec_Python_Code.md)
- [x] Migration applied (alembic upgrade head confirmed at b35a944)
- [x] No hardcoded values

---

## [Engineer] Implementation Log

**Status:** PRE-COMPLETED — Run A merged to master before this /enhance session.

**Branch:** master (merged at 872aeaf; migration fix at b35a944)

**Worktree path:** n/a — changes committed directly to master

**Commit hash:** 872aeaf (Run A merge) + b35a944 (migration fix)

**Files changed:** See [Architect] Design — Files touched table above (12+ files)

**API contract verified:** yes

**Spec updated:** yes — Spec_RITA_App.md and Spec_Python_Code.md updated in 8de4a0c

**Ruff result:** passed

**Definition of Done — Engineer check:**
- [x] API contract matches
- [x] Workflow tier (no JWT)
- [x] Router registered
- [x] Window bindings (deferred Run B)
- [x] Error handled
- [x] Spec updated
- [x] Ruff passes
- [x] No hardcoded values

---

## [QA] Test Results

**Tests written:** 19

**Test file:** `tests/unit/test_instrument_onboard.py`

**Tests passed:** 19 / 19

**Coverage delta:** +19 tests covering both endpoints and the service layer; all yfinance calls mocked (no network traffic). Pytest runtime: 2.84 s.

**Contract check — GET /api/v1/instrument/search (list item fields):**
| Schema field | Handler returns | Match? |
|---|---|---|
| `ticker` | `r.get("symbol", "")` | yes |
| `name` | `r.get("longname") or r.get("shortname", "")` | yes |
| `exchange` | `r.get("exchange", "")` | yes |
| `currency` | `r.get("currency", "")` | yes |
| `country` | `r.get("country", "")` | yes |
| `quote_type` | `"EQUITY"` (hardcoded after filter) | yes |

**Contract check — POST /api/v1/instrument/onboard (response body fields):**
| Schema field | Handler returns | Match? |
|---|---|---|
| `status` | `"ok"` (literal) | yes |
| `ticker` | `body.ticker.upper()` | yes |
| `rows_fetched` | `int` from `fetch_raw_data()` return tuple | yes |
| `rows_seeded` | `int` from `seed_market_cache()` return value | yes |
| `raw_path` | `str(raw_path)` from `fetch_raw_data()` | yes |
| `input_path` | `str(input_path)` from `process_to_input()` | yes |

**Edge cases tested:**
- `q` blank or length < 2 → 400: tested (test_search_q_too_short_returns_400, test_search_q_whitespace_only_returns_400)
- yfinance `Search()` raises exception → 502: tested (test_search_yfinance_unreachable_returns_502, test_search_yfinance_timeout_returns_502)
- POST with ticker already in instruments table → 409: tested (test_onboard_duplicate_ticker_returns_409)
- yfinance returns fewer than 100 rows → 400: tested (test_onboard_fewer_than_100_rows_returns_400)
- yfinance network failure on POST /onboard → 502: tested (test_onboard_yfinance_unreachable_returns_502)
- Missing required body fields → 422: tested (test_onboard_missing_required_fields_returns_422)
- `lot_size` optional (None accepted) → 200: tested (test_onboard_lot_size_is_optional)
- Ticker uppercased in response: tested (test_onboard_ticker_is_uppercased_in_response)
- Service-layer EQUITY filter (non-EQUITY quotes dropped): tested (test_search_tickers_output_keys_match_contract)

**Fix applied:** Three tests failed on initial run due to wrong patch targets. The router uses `from ... import search_tickers` (bound reference), so the two 502 search tests were patching `rita.services.instrument_onboard.search_tickers` (no effect) — corrected to `rita.api.v1.workflow.instrument_onboard.search_tickers`. The service contract test tried to patch `rita.services.instrument_onboard.yf` which doesn't exist (yfinance is imported locally inside the function body) — corrected to patch `yfinance.Search` directly. All 19 pass after fixes.

**Definition of Done — QA check:**
- [x] At least 1 unit test per new endpoint
- [x] All tests pass (19/19)
- [x] API-frontend contract verified (no field name mismatches — all 6 search fields + all 6 onboard fields present)
- [x] Edge cases from Architect section covered (all 5 Architect-specified edge cases tested; 4 additional bonus cases added)

---

## [TechWriter] Documentation

**Confluence page updated:** https://ravionics.atlassian.net/wiki/spaces/RIIAProjec/pages/76611602/Engineering

**Page section modified:** API Endpoint Inventory table — two new rows inserted (GET /api/v1/instrument/search, POST /api/v1/instrument/onboard); page header date bumped to 2026-05-18

**Spec file confirmed current:** yes — Spec_RITA_App.md (line 74), Spec_Python_Code.md (lines 65, 424, 426), and Spec_Data.md (load_ohlcv_csv rename note + find_instrument_csv glob note) all confirmed current; no edits required

**Task brief archived:** yes — file saved as task-brief-20260518-0545.md

**Definition of Done — TechWriter check:**
- [x] Confluence page reflects new feature
- [x] Spec_RITA_App.md endpoint inventory is accurate
- [x] Spec_JS_Code.md module structure table is accurate (Run B JS module deferred; no JS changes in Run A)
- [x] Task brief saved as completed artifact

---

*Brief generated by /enhance orchestrator. Each section written by the designated agent role.*
