# Task Brief — 20260604-1126
**Run ID:** 20260604-1126
**Status:** engineer-complete

---

## Request
F30 Phase 2 — state + initApp refactor, Real/Mock toggle

## App Target
fno

## Skill Selected
`project-office/skills/skill-add-fno-feature.md`

## Spec Reference
- `project-office/specs/Spec_RITA_App.md`
- `project-office/specs/Spec_JS_Code.md`

---

## [Architect] Design

**Feature summary:** Phase 2 wires the Phase 1 backend endpoint (`/api/v1/experience/fno/portfolio-analytics?mode=`) to the FnO dashboard frontend. Replaces the existing fragmented multi-fetch `initApp()` chain with a single call to the unified endpoint, maps all 13 response fields to state, and adds a Real/Mock sidebar toggle.

**API contract:**
- Endpoint: `GET /api/v1/experience/fno/portfolio-analytics?mode=real|mock`
- No request body. JWT bearer token required only for `mode=real`.
- Response: `PortfolioAnalyticsResponse` — 13 fields: `portfolio_meta`, `market`, `positions`, `greeks`, `net_greeks`, `net_delta`, `scenario_levels`, `payoff`, `stress`, `hedge_quality`, `closed_positions`, `realized_pnl`, `margin`.

**Files to touch:**

| File | Change |
|---|---|
| `dashboard/js/fno/state.js` | Add `portfolioMeta: null` and `analyticsMode: 'mock'` fields |
| `dashboard/js/fno/app-init.js` | Replace multi-fetch chain with single `portfolio-analytics` call; `initApp(mode='mock')` signature; fetchPositions shim |
| `dashboard/js/fno/main.js` | Add `window.toggleAnalyticsMode` binding |
| `dashboard/fno.html` | Add Real/Mock toggle div after `#sidebar-as-of` |
| `project-office/specs/Spec_JS_Code.md` | Update `app-init.js` row |
| `project-office/specs/Spec_RITA_App.md` | Annotate `portfolio-analytics` row with Phase 2 JS consumer note |

**DoD checklist:**
1. `state.portfolioMeta` and `state.analyticsMode` added to state.js
2. `initApp(mode='mock')` single-fetch replacing old chain
3. All 13 fields mapped to state
4. `fetchPositions` shim preserved (main.js imports it)
5. `window.toggleAnalyticsMode` in main.js
6. Real/Mock toggle HTML in fno.html sidebar
7. Spec_JS_Code.md app-init.js row updated
8. Spec_RITA_App.md portfolio-analytics row annotated
9. Ruff passes (no Python changes in Phase 2)

---

## [Engineer] Implementation Log

**Branch:** worktree-agent-a73616c67686a1051
**Worktree path:** /Users/sgawde/work/riia-jun-demo/riia-cowork-jun-demo/.claude/worktrees/agent-a73616c67686a1051
**Commit hash:** 3c19040
**Files changed:**
- `riia-jun-release/dashboard/js/fno/state.js` — added `portfolioMeta: null`, `analyticsMode: 'mock'`
- `riia-jun-release/dashboard/js/fno/app-init.js` — full refactor: single fetch to portfolio-analytics, `initApp(mode='mock')` signature, removed loadEquityHedge/injectAsmlToState/multi-fetch chain, added _renderAll() helper, fetchPositions shim, disable/re-enable chk toggle
- `riia-jun-release/dashboard/js/fno/main.js` — added `window.toggleAnalyticsMode`
- `riia-jun-release/dashboard/fno.html` — added Real/Mock toggle + error div after `#sidebar-as-of`
- `project-office/specs/Spec_JS_Code.md` — updated app-init.js row with new signature
- `project-office/specs/Spec_RITA_App.md` — annotated portfolio-analytics row with Phase 2 JS consumer note

**API contract verified:** yes — all 13 fields from `PortfolioAnalyticsResponse` mapped: `portfolio_meta`, `market`, `positions`, `greeks`, `net_greeks`, `net_delta`, `scenario_levels`, `payoff`, `stress`, `hedge_quality`, `closed_positions`, `realized_pnl`, `margin`

**Spec updated:** yes — both Spec_JS_Code.md (app-init.js row) and Spec_RITA_App.md (portfolio-analytics Phase 2 annotation)

**Ruff result:** passed (no Python changes in Phase 2)

**DoD checklist:**
- [x] API contract matches — all 13 Pydantic fields mapped to state fields exactly
- [x] Correct tier used — Experience tier (`/api/v1/experience/fno/portfolio-analytics`)
- [x] Section loader registered — n/a (Phase 2 is an init refactor, not a new section)
- [x] Window bindings set — `window.toggleAnalyticsMode` added in main.js
- [x] Error handled — raw fetch with status discrimination: 401→login error, 404→no portfolio, other→API error; disable/re-enable toggle; _renderAll() on error path
- [x] Spec updated — Spec_RITA_App.md portfolio-analytics row + Spec_JS_Code.md app-init.js row
- [x] HTML changes complete — Real/Mock toggle added to fno.html sidebar (grep → read ±20 lines → targeted Edit)
- [x] Ruff passes — confirmed: "All checks passed!"
- [x] No hardcoded values — uses `apiBase()` from api.js, no localhost URLs, no hardcoded lot sizes

**FC gates:**
- FC-004 (field contract): all 13 `data.field` reads match Pydantic schema field names exactly ✓
- FC-IMP (named imports): all imports verified from prior run (no new imports added) ✓
- FC-TIER: no banned system-tier calls in changed files ✓
- FC-API-SIG: no `api(path, {` patterns in changed files ✓
- FC-MERGE: no conflict markers in dashboard/js/ ✓

**Notes:**
- Worktree was rebased onto master before implementation to pick up F30 Phase 1 backend code
- `loadEquityHedge`/`rita:asml-state-updated` event listener removed from initApp (per G5 in eng-context: call site removed, not the function)
- `saveToday()`/`syncPriceHistory()` retained (only called after successful data load, not on error path)
- `apiFetch` from shared api.js does not expose status codes; used raw `fetch()` directly for 401/404 discrimination as required by task brief
