# Task Brief — RITA Page Swaps: Overview Landing

**Date:** 2026-05-11
**Feature:** Swap Market Signals to be the landing page (renamed "Overview"); demote old Overview to "Model Overview" in Phase 03.
**Type:** Frontend nav/section reorganisation — no backend changes.

---

## [Architect] Design

### Summary of Changes

| File | Change |
|---|---|
| `dashboard/js/rita/nav.js` | `_currentSection` default: `'home'` → `'market-signals'` |
| `dashboard/js/rita/main.js` | Add `loadMarketSignals()` to `window.load` handler; update comment |
| `dashboard/rita.html` | Nav labels, nav order, active class swap, instrument selector tab move |
| `project-office/specs/Spec_RITA_App.md` | Nav order table in Section 12; Flow 1 entry point note |
| `project-office/specs/Spec_JS_Code.md` | nav.js description; landing section loader rule |
| `riia-jun-release/tests/e2e/test_rita_scenarios.py` | Comment-only: Overview → Model Overview |

### HTML Changes (rita.html)

- `sec-market-signals` nav item: label "Market Signals" → "Overview"; moved to first position in Phase 01; gets `active` class on nav item.
- `sec-home` nav item: label "Overview" → "Model Overview"; removed from standalone top group; moved into Phase 03 — Analyse (after Explainability).
- `sec-market-signals` section: `class="section"` → `class="section active"` (visible on first paint).
- `sec-home` section: `class="section active"` → `class="section"` (hidden by default).
- Instrument selector `.inst-tab` buttons: moved from inside `sec-home` to inside `sec-market-signals` (before timeframe tabs).
- Page titles updated: sec-market-signals → "Overview"; sec-home → "Model Overview".

### Definition of Done Checklist (Architect)

1. `_currentSection` default = `'market-signals'` in nav.js
2. `loadMarketSignals()` called in `window.load` handler in main.js
3. Nav label for sec-market-signals = "Overview" (first Phase 01 item)
4. Nav label for sec-home = "Model Overview" (in Phase 03)
5. `sec-market-signals` has `active` class in HTML; `sec-home` does not
6. `.inst-tab` buttons live inside `sec-market-signals`
7. Specs updated (Spec_RITA_App.md + Spec_JS_Code.md)
8. No backend changes; no new endpoints; ruff passes

---

## [Engineer] Implementation Log

**Branch:** `worktree-agent-a544d25f28206936c`
**Worktree path:** `C:\Users\Sandeep\Documents\Work\code\riia-cowork-jun\.claude\worktrees\agent-a544d25f28206936c`

**Files changed:**
- `riia-jun-release/dashboard/js/rita/nav.js` — `_currentSection` default changed to `'market-signals'`
- `riia-jun-release/dashboard/js/rita/main.js` — `loadMarketSignals()` added to `window.load`; comment updated
- `riia-jun-release/dashboard/rita.html` — nav labels, nav order, active class, instrument selector move, page-title updates, CSS comment update
- `project-office/specs/Spec_RITA_App.md` — Nav order table added to Section 12; Flow 1 entry note updated
- `project-office/specs/Spec_JS_Code.md` — nav.js description updated; landing section loader rule added
- `riia-jun-release/tests/e2e/test_rita_scenarios.py` — comments/docstrings: Overview → Model Overview

**Commit hash:** `cf705bc`

**API contract verified:** Yes — no new or modified endpoints; existing `loadMarketSignals()` wiring preserved.

**Spec updated:** Yes — both `Spec_RITA_App.md` and `Spec_JS_Code.md` updated.

**Ruff result:** Passed (all checks passed, no Python source changed).

### DoD Checklist

| # | Item | Status |
|---|---|---|
| 1 | `_currentSection` default = `'market-signals'` in nav.js | PASS |
| 2 | `loadMarketSignals()` called in `window.load` handler | PASS |
| 3 | Nav label for sec-market-signals = "Overview" (first Phase 01 item) | PASS |
| 4 | Nav label for sec-home = "Model Overview" (in Phase 03) | PASS |
| 5 | `sec-market-signals` has `active` class; `sec-home` does not | PASS |
| 6 | `.inst-tab` buttons moved into `sec-market-signals` | PASS |
| 7 | Specs updated (Spec_RITA_App.md + Spec_JS_Code.md) | PASS |
| 8 | No backend changes; ruff passes | PASS |

**DoD result: 8/8 passed.**
